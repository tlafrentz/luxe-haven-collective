import { AdminUnavailableView } from "@/components/admin/admin-empty-view";
export default function ProviderHealthPage() { return <AdminUnavailableView title="Provider Health" description="Monitor the health of connected providers." capability="Central health thresholds, uptime windows, and incident telemetry are not configured." />; }
