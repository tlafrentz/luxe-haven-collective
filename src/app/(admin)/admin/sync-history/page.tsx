import { AdminUnavailableView } from "@/components/admin/admin-empty-view";
export default function SyncHistoryPage() { return <AdminUnavailableView title="Sync History" description="View synchronization attempts and results." capability="A unified immutable synchronization-attempt projection is not configured. Existing provider-specific history remains available on Integrations." />; }
