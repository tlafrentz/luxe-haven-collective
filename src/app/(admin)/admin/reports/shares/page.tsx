import { createClient } from "@/lib/supabase/server";
export default async function ReportSharesPage(){const client=await createClient(),{data}=await client.from("report_shares").select("id,report_id,status,access_mode,expires_at,max_views,view_count,created_at,last_viewed_at").order("created_at",{ascending:false}).limit(200);return <main className="mx-auto max-w-7xl space-y-8 px-5 py-10"><header><p className="eyebrow">Report operations</p><h1 className="mt-2 text-4xl font-semibold">External shares</h1><p className="mt-3 text-stone-600">Report-scoped access metadata. Raw tokens are never stored or displayed.</p></header><div className="overflow-x-auto rounded-2xl border"><table className="min-w-full text-left text-sm"><thead className="bg-stone-50"><tr><th className="p-4">Report</th><th className="p-4">State</th><th className="p-4">Access</th><th className="p-4">Views</th><th className="p-4">Expires</th><th className="p-4">Last viewed</th></tr></thead><tbody>{(data??[]).map(share=><tr className="border-t" key={share.id}><td className="p-4">{share.report_id}</td><td className="p-4">{share.status}</td><td className="p-4">{share.access_mode}</td><td className="p-4">{share.view_count}{share.max_views?` / ${share.max_views}`:""}</td><td className="p-4">{share.expires_at?new Date(share.expires_at).toLocaleString():"Never"}</td><td className="p-4">{share.last_viewed_at?new Date(share.last_viewed_at).toLocaleString():"Never"}</td></tr>)}</tbody></table></div></main>;}
