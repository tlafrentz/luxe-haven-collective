import { retryReportJobAction } from "@/app/actions/reporting";import { createClient } from "@/lib/supabase/server";
export default async function ReportJobsPage(){const client=await createClient(),{data}=await client.from("report_generation_jobs").select("id,report_request_id,generated_report_id,status,stage,attempts,failure_code,failure_message,created_at,lease_expires_at").order("created_at",{ascending:false}).limit(200);return <main className="mx-auto max-w-7xl space-y-8 px-5 py-10"><header><p className="eyebrow">Report operations</p><h1 className="mt-2 text-4xl font-semibold">Generation jobs</h1><p className="mt-3 text-stone-600">Lease-protected, idempotent generation and bounded retry recovery.</p></header><div className="overflow-x-auto rounded-2xl border"><table className="min-w-full text-left text-sm"><thead className="bg-stone-50"><tr><th className="p-4">Request</th><th className="p-4">Status</th><th className="p-4">Stage</th><th className="p-4">Attempts</th><th className="p-4">Failure</th><th className="p-4">Recovery</th></tr></thead><tbody>{(data??[]).map(job=><tr className="border-t" key={job.id}><td className="p-4">{job.report_request_id}</td><td className="p-4">{job.status}</td><td className="p-4">{job.stage}</td><td className="p-4">{job.attempts}</td><td className="p-4">{job.failure_code??"—"}</td><td className="p-4">{job.status==="failed"?<form action={retryReportJobAction}><input name="jobId" type="hidden" value={job.id}/><button className="rounded-full border px-3 py-1 font-semibold">Retry eligible job</button></form>:"—"}</td></tr>)}</tbody></table></div></main>;}
