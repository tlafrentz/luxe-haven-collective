import Link from"next/link";export default async function AdminActivationsPage({searchParams}:{searchParams:Promise<{view?:string}>}){const{view="needs_attention"}=await searchParams;const filters=["needs_attention","customer_action","internal_action","system_processing","blocked","unassigned","recently_activated","first_value_achieved","inactive","all"];return <main className="mx-auto max-w-7xl space-y-8 px-5 py-10"><header><p className="eyebrow">Activation operations</p><h1 className="mt-2 text-4xl font-semibold">Customer activations</h1><p className="mt-3 max-w-3xl text-stone-600">A tenant-safe operational view of commercial activation, onboarding, first value, and product readiness. Reading this queue never creates or changes upstream records.</p></header><nav aria-label="Activation queue filters" className="flex flex-wrap gap-2">{filters.map(filter=><Link key={filter} href={`/admin/activations?view=${filter}`} aria-current={view===filter?"page":undefined} className={`rounded-full border px-4 py-2 text-sm font-semibold ${view===filter?"bg-emerald-950 text-white":"bg-white"}`}>{filter.replaceAll("_"," ")}</Link>)}</nav><section className="rounded-2xl border bg-white p-10 text-center"><h2 className="text-xl font-semibold">No deployed activation projection</h2><p className="mt-2 text-stone-600">The control-plane route is ready. Cases appear only after the CA-001A–D migrations and authorized projection adapter are deployed.</p></section></main>}
