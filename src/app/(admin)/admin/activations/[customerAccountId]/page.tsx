import Link from"next/link";export default async function AdminActivationCasePage({params}:{params:Promise<{customerAccountId:string}>}){const{customerAccountId}=await params;return <main className="mx-auto max-w-7xl space-y-8 px-5 py-10"><header><Link href="/admin/activations" className="text-sm font-semibold text-emerald-800">← Activation queue</Link><p className="mt-6 eyebrow">Activation operations</p><h1 className="mt-2 text-4xl font-semibold">Activation unavailable</h1><p className="mt-3 text-stone-600">No authorized canonical activation projection was resolved for account reference <span className="font-mono text-xs">{customerAccountId}</span>.</p></header><section role="status" className="rounded-2xl border border-amber-200 bg-amber-50 p-6"><h2 className="font-semibold">Safe unavailable state</h2><p className="mt-2 text-sm text-stone-700">This page does not create journeys, onboarding cases, entitlements, or product artifacts. Deploy the authorized projection adapter before operational use.</p></section></main>}
