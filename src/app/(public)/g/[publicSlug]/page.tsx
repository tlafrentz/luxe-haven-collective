import type{Metadata}from"next";import{redirect}from"next/navigation";import{getPublicGuidebookDeliveryRequest}from"@/app/actions/guidebook-delivery";import{PublicGuidebookExperience}from"@/components/guidebooks/public-guidebook-experience";import{guidebookPublicRenderer,type GuidebookArtifactPayload,type PublicGuidebookView}from"@/features/guidebook-studio";import{ArtifactRenderingEngine,type PublishedArtifactEnvelope}from"@/platform/artifact-rendering";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{publicSlug:string}>}):Promise<Metadata>{const result=await getPublicGuidebookDeliveryRequest((await params).publicSlug);if(result.state!=="active")return{title:"Guest Guide | Luxe Haven Collective",robots:{index:false,follow:false}};return{title:"Your Guest Guide | Luxe Haven Collective",description:"Everything you need for a comfortable stay.",robots:{index:false,follow:false}}}
export default async function PublicGuidebookPage({params,searchParams}:{params:Promise<{publicSlug:string}>;searchParams:Promise<{source?:string}>}){
 const{publicSlug}=await params,{source}=await searchParams,result=await getPublicGuidebookDeliveryRequest(publicSlug);if(result.state==="redirect")redirect(`/g/${result.slug}`);if(result.state!=="active")return<PublicGuidebookUnavailable/>;let guidebook:PublicGuidebookView|null=null;try{guidebook=new ArtifactRenderingEngine().register(guidebookPublicRenderer).render<GuidebookArtifactPayload,PublicGuidebookView>(result.envelope as PublishedArtifactEnvelope<GuidebookArtifactPayload>)}catch{}if(!guidebook)return<PublicGuidebookUnavailable/>;return<PublicGuidebookExperience slug={publicSlug} guidebook={guidebook} source={source==="qr"?"qr":"link"}/>;
}
function PublicGuidebookUnavailable(){return<main className="grid min-h-screen place-items-center bg-[#f2ede4] p-6"><section role="status" className="w-full max-w-lg rounded-[2rem] border border-stone-200 bg-[#fbf8f1] p-8 text-center shadow-xl sm:p-12"><p className="text-xs font-bold uppercase tracking-[.22em] text-amber-700">Luxe Haven Collective</p><h1 className="mt-6 font-serif text-4xl text-stone-950">This guidebook is unavailable.</h1><p className="mt-5 leading-7 text-stone-600">Check the link or contact your host through your booking channel.</p></section></main>}
