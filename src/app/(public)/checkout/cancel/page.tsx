import Link from"next/link";export default async function CheckoutCancelPage({searchParams}:{searchParams:Promise<{order?:string}>}){const{order}=await searchParams;return <main className="mx-auto max-w-2xl px-5 py-20 text-center"><p className="eyebrow">Checkout cancelled</p><h1 className="mt-3 font-serif text-5xl">No payment was confirmed</h1><p className="mt-5 text-stone-600">Your pending order remains available for a future retry and no entitlement has been activated.</p>{order?<p className="mt-5 text-sm text-stone-500">Order reference: {order}</p>:null}<Link href="/pricing" className="mt-8 inline-flex rounded-full bg-stone-950 px-6 py-3 font-semibold text-white">Return to pricing</Link></main>}
