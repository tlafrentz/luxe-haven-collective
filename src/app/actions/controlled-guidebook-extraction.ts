"use server";
import "server-only";
import {redirect} from "next/navigation";
import {z} from "zod";
import {requireRole} from "@/lib/auth/session";
import {createAdminClient} from "@/lib/supabase/admin";
import {createClient} from "@/lib/supabase/server";
import {advanceControlledExtraction,authorizeControlledJourneyActor,CONTROLLED_EXTRACTION_OPERATION,CONTROLLED_STAGES} from "@/features/guidebook-creation-assistant/controlled-journey";

const path="/admin/guidebooks/controlled-extraction-verification",selection=z.object({customerAccountId:z.string().uuid(),entitlementId:z.string().uuid()});
async function actor(){const auth=await requireRole(["admin"]);if(!await authorizeControlledJourneyActor(auth.user.id))throw new Error("CONTROLLED_EXTRACTION_CAPABILITY_REQUIRED");return auth}

export async function getControlledExtractionLaunchProjection(){await actor();const db=await createClient(),now=new Date().toISOString(),{data:identities,error:identityError}=await db.from("controlled_verification_identities").select("customer_account_id").eq("environment_code","production").eq("identity_type_code","guidebook_customer").eq("status","active").contains("allowed_scenario_codes",["PV-009"]),ids=[...new Set((identities??[]).map(row=>String(row.customer_account_id)).filter(Boolean))];if(identityError)return{customers:[],launch:null,emptyReason:"The Admin session cannot read controlled verification customers."};if(!ids.length)return{customers:[],launch:null,emptyReason:"No active controlled verification customer is registered for PV-009."};const[{data:accounts,error:accountError},{data:grants,error:grantError},{data:allocations,error:allocationError},{data:launch}]=await Promise.all([db.from("customer_accounts").select("id,tenant_id,display_name,status").in("id",ids).eq("status","active").order("display_name"),db.from("commercial_entitlements").select("id,customer_account_id,tenant_id,capability_code,effective_from,effective_until,revision,resource_scope_type,resource_scope_id,status").in("customer_account_id",ids).in("capability_code",["guidebook.create","guidebook.property.create_standalone"]).eq("status","active").lte("effective_from",now).or(`effective_until.is.null,effective_until.gt.${now}`),db.from("property_entitlement_allocations").select("entitlement_id").in("customer_account_id",ids).eq("status","reserved"),db.from("guidebook_controlled_extraction_launches").select("status,correlation_id,created_at,closed_at").eq("operation_code",CONTROLLED_EXTRACTION_OPERATION).maybeSingle()]);if(accountError||grantError||allocationError)return{customers:[],launch:null,emptyReason:"The authorized Admin projection could not load canonical customers, grants, or allocations."};const reserved=new Set((allocations??[]).map(row=>String(row.entitlement_id))),customers=(accounts??[]).map(account=>({id:String(account.id),name:String(account.display_name??"Controlled customer"),entitlements:(grants??[]).filter(grant=>grant.customer_account_id===account.id&&grant.tenant_id===account.tenant_id&&!reserved.has(String(grant.id))&&((grant.resource_scope_type==="customer_account"&&grant.resource_scope_id===account.id)||(grant.resource_scope_type==="workspace"&&grant.resource_scope_id===account.tenant_id))).map(grant=>({id:String(grant.id),label:`${grant.capability_code} · ${grant.resource_scope_type} scope · revision ${Number(grant.revision)}`,effectiveUntil:grant.effective_until?String(grant.effective_until):null}))}));return{customers,launch:launch?{status:String(launch.status),correlationId:String(launch.correlation_id),createdAt:String(launch.created_at),closedAt:launch.closed_at?String(launch.closed_at):null}:null,emptyReason:customers.some(customer=>customer.entitlements.length)?null:"Controlled customers are active, but no active, unexpired, unrevoked, unallocated Guidebook grant is eligible."}}

export async function launchControlledExtractionAction(form:FormData){const {user}=await actor(),parsed=selection.safeParse(Object.fromEntries(form.entries()));if(!parsed.success)redirect(`${path}?result=invalid-selection`);const db=createAdminClient(),now=new Date().toISOString(),customer=await db.from("customer_accounts").select("id,tenant_id,status").eq("id",parsed.data.customerAccountId).eq("status","active").single(),identity=await db.from("controlled_verification_identities").select("id").eq("environment_code","production").eq("identity_type_code","guidebook_customer").eq("customer_account_id",parsed.data.customerAccountId).eq("status","active").contains("allowed_scenario_codes",["PV-009"]).maybeSingle(),grant=await db.from("commercial_entitlements").select("id,tenant_id,customer_account_id,capability_code,status,effective_from,effective_until,resource_scope_type,resource_scope_id").eq("id",parsed.data.entitlementId).single();if(!customer.data||!identity.data||!grant.data||grant.data.tenant_id!==customer.data.tenant_id||grant.data.customer_account_id!==customer.data.id||!["guidebook.create","guidebook.property.create_standalone"].includes(grant.data.capability_code)||grant.data.status!=="active"||!((grant.data.resource_scope_type==="customer_account"&&grant.data.resource_scope_id===customer.data.id)||(grant.data.resource_scope_type==="workspace"&&grant.data.resource_scope_id===customer.data.tenant_id))||String(grant.data.effective_from)>now||(grant.data.effective_until&&String(grant.data.effective_until)<=now))redirect(`${path}?result=ineligible`);
 const reserved=await db.from("property_entitlement_allocations").select("id").eq("entitlement_id",grant.data.id).eq("status","reserved").maybeSingle();if(reserved.data)redirect(`${path}?result=grant-already-allocated`);const candidate=await db.from("production_release_candidates").select("id").eq("environment_code","production").order("deployed_at",{ascending:false}).limit(1).single(),reviewer=await db.from("controlled_verification_identities").select("opaque_auth_subject_reference").eq("environment_code","production").eq("identity_type_code","release_reviewer").eq("status","active").neq("opaque_auth_subject_reference",user.id).or(`expires_at.is.null,expires_at.gt.${now}`).limit(1).single();if(!candidate.data||!reviewer.data)redirect(`${path}?result=verification-context-unavailable`);const correlationId=crypto.randomUUID(),created=await db.rpc("create_ca001f_verification_run",{p_candidate_id:candidate.data.id,p_actor_id:user.id,p_reviewer_id:reviewer.data.opaque_auth_subject_reference,p_correlation_id:correlationId,p_instances:[{scenario_code:"PV-009",scenario_version:1,expected_outcome_code:"CONTROLLED_EXTRACTION_RETAINED"}]});if(created.error||!created.data)redirect(`${path}?result=run-create-failed`);const run=created.data as Record<string,unknown>,armed=await db.from("guidebook_controlled_extraction_launches").insert({operation_code:CONTROLLED_EXTRACTION_OPERATION,verification_run_id:String(run.id),actor_id:user.id,customer_account_id:customer.data.id,entitlement_id:grant.data.id,status:"armed",correlation_id:correlationId});if(armed.error)redirect(`${path}?result=already-claimed`);await db.from("guidebook_controlled_extraction_launches").update({status:"running"}).eq("verification_run_id",run.id).eq("status","armed");
 let succeededCorrelation:string|null=null;try{let result=await advanceControlledExtraction({actorId:user.id,authenticated:await createClient(),command:{expectedStage:"not_started",verificationRunId:String(run.id),customerAccountId:customer.data.id,entitlementId:grant.data.id}});while(result.stage!=="completed"){if(!CONTROLLED_STAGES.includes(result.stage))throw new Error("CONTROLLED_EXTRACTION_STATE_INVALID");result=await advanceControlledExtraction({actorId:user.id,authenticated:await createClient(),command:{expectedStage:result.stage}})}await db.from("guidebook_controlled_extraction_launches").update({status:"closed_succeeded",closed_at:new Date().toISOString()}).eq("verification_run_id",run.id).eq("status","running");succeededCorrelation=result.correlationId}catch{await db.from("guidebook_controlled_extraction_launches").update({status:"cleanup_required"}).eq("verification_run_id",run.id).eq("status","running");try{await advanceControlledExtraction({actorId:user.id,authenticated:await createClient(),command:{action:"cleanup"}});await db.from("guidebook_controlled_extraction_launches").update({status:"closed_failed",closed_at:new Date().toISOString()}).eq("verification_run_id",run.id).eq("status","cleanup_required")}catch{}}if(succeededCorrelation)redirect(`${path}?result=succeeded&correlation=${encodeURIComponent(succeededCorrelation)}`);redirect(`${path}?result=failed`)}

export async function cleanupControlledExtractionAction(){const {user}=await actor(),db=createAdminClient(),{data:launch}=await db.from("guidebook_controlled_extraction_launches").select("verification_run_id,status").eq("operation_code",CONTROLLED_EXTRACTION_OPERATION).eq("actor_id",user.id).maybeSingle();if(!launch||!["running","cleanup_required"].includes(String(launch.status)))redirect(`${path}?result=cleanup-not-required`);let cleaned=false;try{await advanceControlledExtraction({actorId:user.id,authenticated:await createClient(),command:{action:"cleanup"}});await db.from("guidebook_controlled_extraction_launches").update({status:"closed_failed",closed_at:new Date().toISOString()}).eq("verification_run_id",launch.verification_run_id).in("status",["running","cleanup_required"]);cleaned=true}catch{}redirect(`${path}?result=${cleaned?"cleaned":"cleanup-blocked"}`)}
