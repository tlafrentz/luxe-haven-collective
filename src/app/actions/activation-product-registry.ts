"use server";import"server-only";import{requireRole}from"@/lib/auth/session";import{createAdminClient}from"@/lib/supabase/admin";import{RegisterActivationProductDefinitions}from"@/platform/onboarding";export async function registerActivationProductDefinitions(){const{user}=await requireRole(["admin"]);return new RegisterActivationProductDefinitions(createAdminClient()).execute(user.id)}
