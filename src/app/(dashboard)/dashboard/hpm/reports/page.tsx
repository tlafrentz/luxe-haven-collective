import { HpmFailure, HpmReportCatalog, HpmWorkspaceFrame, listAuthorizedHpmReports, parseHpmWorkspaceQuery } from "@/features/hpm-workspace";
export default async function HpmReportsPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const query = parseHpmWorkspaceQuery(await searchParams), result = await listAuthorizedHpmReports(query); if (!result.ok) return <HpmFailure {...result} />; return <HpmWorkspaceFrame activeHref="/dashboard/hpm/reports" query={query} model={result.workspace}><HpmReportCatalog definitions={result.definitions} query={query} /></HpmWorkspaceFrame>; }
