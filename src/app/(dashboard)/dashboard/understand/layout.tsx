import { IntelligenceWorkspaceHeader } from "@/components/intelligence-workspace-navigation";
export default function UnderstandLayout({ children }: { children: React.ReactNode }) { return <><IntelligenceWorkspaceHeader stage="understand" />{children}</>; }
