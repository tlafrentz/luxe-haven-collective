import Link from "next/link";
import { getExecutiveIntelligenceView } from "@/features/executive-intelligence";
import { resolveAnalyticsDateRange } from "@/features/analytics";
import { understandRoutes } from "@/platform/experience";
type SearchParams = Promise<{property?:string;start?:string;end?:string;from?:string;to?:string;returnTo?:string}>;
export default async function DataQualityPage({searchParams}:Readonly<{searchParams:SearchParams}>) { const params=await searchParams; const range=resolveAnalyticsDateRange({startDate:params.from??params.start,endDate:params.to??params.end}); const {view}=await getExecutiveIntelligenceView({propertyId:params.property??null,startDate:range.startDate,endDate:range.endDate}); const quality=view.dataQuality,total=quality.availablePillars.length+quality.unavailablePillars.length,coverage=total?Math.round(quality.availablePillars.length/total*100):0; const returnTo=safeReturn(params.returnTo,understandRoutes.portfolio); return <main className="mx-auto max-w-[1200px] space-y-6 px-4 pb-10 pt-3 sm:px-6 lg:px-8"><header className="rounded-[2rem] border border-stone-200 bg-white p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-violet-700">Understand › Portfolio Intelligence › Data Quality</p><h1 className="mt-3 text-3xl font-semibold">Portfolio Data Quality</h1><p className="mt-2 text-sm text-stone-600">{quality.summary}</p><p className="mt-5 text-4xl font-semibold">{coverage}% <span className="text-base font-normal text-stone-500">coverage</span></p></header><section className="grid gap-5 md:grid-cols-2"><QualityList title="Available evidence" items={quality.availablePillars} available/><QualityList title="Insufficient evidence" items={quality.unavailablePillars}/></section><section className="rounded-[2rem] border border-stone-200 bg-white p-6"><h2 className="text-xl font-semibold">What needs attention</h2>{quality.gaps.length?<ul className="mt-4 divide-y">{quality.gaps.map((gap,index)=><li key={`${gap.type}:${index}`} className="py-4"><p className="font-semibold capitalize">{gap.type.replaceAll("-"," ")}</p><p className="mt-1 text-sm text-stone-600">{gap.message}</p></li>)}</ul>:<p className="mt-3 text-sm text-stone-600">No material evidence gaps were found.</p>}<div className="mt-5 flex flex-wrap gap-3"><Link href={returnTo} className="rounded-md border px-4 py-3 text-sm font-semibold">← Return to Portfolio Intelligence</Link><Link href={`/dashboard/workspace/connected-systems?returnTo=${encodeURIComponent(understandRoutes.portfolioDataQuality)}`} className="rounded-md bg-stone-950 px-4 py-3 text-sm font-semibold text-white">Review data sources</Link></div></section></main> }
function QualityList({title,items,available=false}:{title:string;items:readonly string[];available?:boolean}){return <section className="rounded-[2rem] border border-stone-200 bg-white p-6"><h2 className="text-xl font-semibold">{title}</h2>{items.length?<ul className="mt-4 space-y-3">{items.map(item=><li key={item} className="flex items-center gap-3 rounded-xl bg-stone-50 p-4 text-sm font-semibold"><span className={available?"text-emerald-700":"text-amber-700"}>{available?"●":"○"}</span><span className="capitalize">{item.replaceAll("-"," ")}</span></li>)}</ul>:<p className="mt-3 text-sm text-stone-600">None.</p>}</section>}
function safeReturn(value:string|undefined,fallback:string){return value?.startsWith("/dashboard/understand/portfolio")&&!value.startsWith("//")?value:fallback}
