import { getFinancialPlanningRouteState } from "@/app/actions/financial-planning-runtime";
import type { FinancialPeriodPreset } from "@/app/actions/financial-overview-runtime";
import { FinancialPlanningErrorView, FinancialPlanningPage } from "@/features/financial-intelligence/presentation";
export default async function PlanningPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const params=await searchParams,scope=single(params.scope),result=await getFinancialPlanningRouteState({workspaceId:single(params.workspace),propertyIds:scope==="workspace"||!params.properties?undefined:values(params.properties),periodPreset:preset(single(params.period)),customFrom:single(params.from),customTo:single(params.to)});return result.ok?<FinancialPlanningPage planning={result.planning}/>:<FinancialPlanningErrorView code={result.code} message={result.message}/>}
function single(value:string|string[]|undefined){return Array.isArray(value)?value[0]:value}function values(value:string|string[]){return Array.isArray(value)?value:[value]}function preset(value?:string):FinancialPeriodPreset{return["this-month","last-month","qtd","ytd","12m","custom"].includes(value??"")?value as FinancialPeriodPreset:"ytd"}
