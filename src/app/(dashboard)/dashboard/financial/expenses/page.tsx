import{getFinancialExpenseWorkspaceRouteState}from"@/app/actions/financial-expense-workspace-runtime";import{getFinancialOverviewRouteState}from"@/app/actions/financial-overview-runtime";import{WorkspaceHeader,WorkspacePage}from"@/components/application-layout";import{OperatingExpensesWorkspace}from"@/features/financial-intelligence/presentation";
export default async function ExpensesPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const params=await searchParams,single=(value:string|string[]|undefined)=>Array.isArray(value)?value[0]:value,overview=await getFinancialOverviewRouteState({workspaceId:single(params.workspace),periodPreset:"custom",customFrom:single(params.from),customTo:single(params.to),comparisonType:"previous-period"});if(!overview.ok)return <WorkspacePage><p role="alert">{overview.message}</p></WorkspacePage>;const result=await getFinancialExpenseWorkspaceRouteState({workspaceId:overview.overview.identity.workspaceId,propertyIds:overview.overview.scope.propertyIds,from:overview.overview.period.from,to:overview.overview.period.to,basis:"actual"});return <WorkspacePage>{result.ok?<OperatingExpensesWorkspace initialExpenses={result.workspace.expenses} properties={result.workspace.properties} workspaceId={overview.overview.identity.workspaceId} currency={overview.overview.identity.reportingCurrency} basis="actual" canManage={result.canManage} initialView={(single(params.expenseView)as"list"|"category"|"recurring")??"list"}/>:<><WorkspaceHeader eyebrow="Financial Intelligence · Manage" title="Operating Expenses" description="Search, review, categorize, and manage operating expenses."/><p className="mt-8" role="alert">{result.message}</p></>}</WorkspacePage>}
