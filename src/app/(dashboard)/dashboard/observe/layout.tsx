import { IntelligenceWorkspaceHeader } from "@/components/intelligence-workspace-navigation";
export default function ObserveLayout({ children }: { children: React.ReactNode }) { return <><IntelligenceWorkspaceHeader stage="observe" />{children}</>; }
