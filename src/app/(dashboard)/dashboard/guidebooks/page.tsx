import Link from"next/link";
import{getGuidebookStudioRequest}from"@/app/actions/guidebook-studio";
export default async function GuidebookListPage(){const result=await getGuidebookStudioRequest();return <main className="mx-auto max-w-7xl space-y-7 py-8"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-amber-700">Guest experience publishing</p><h1 className="mt-2 text-4xl font-semibold">Guidebook Studio</h1><p className="mt-2 max-w-2xl text-stone-600">Create living, mobile-first guest experiences from canonical Property information.</p></div><Link href="/dashboard/guidebooks/new" className="rounded-full bg-stone-950 px-5 py-3 text-sm font-semibold text-white">Create guidebook</Link></header>{!result.ok?<section role="alert" className="rounded-2xl border border-amber-200 bg-amber-50 p-6">Guidebook Studio is unavailable or permission-limited.</section>:<><section className="grid gap-4 sm:grid-cols-3"><Metric label="Guidebooks" value={result.guidebooks.length}/><Metric label="Published" value={result.guidebooks.filter((g:{status:string})=>g.status==="published").length}/><Metric label="Properties without a guide" value={result.properties.filter((p:{id:string})=>!result.guidebooks.some((g:{property_id:string})=>g.property_id===p.id)).length}/></section>{!result.entitlements.create?<p className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Creating guidebooks requires the <code>guidebooks.create</code> entitlement.</p>:null}{result.guidebooks.length?<section className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{result.guidebooks.map((g:Record<string,unknown>)=><Link key={String(g.id)} href={`/dashboard/guidebooks/${g.id}`} className="rounded-3xl border border-stone-200 bg-white p-6 shadow-sm outline-none hover:border-amber-300 focus-visible:ring-2 focus-visible:ring-amber-600"><p className="text-xs font-semibold uppercase tracking-wide text-amber-700">{String(g.status)} · Version {String(g.current_version)}</p><h2 className="mt-3 text-xl font-semibold">{String(g.title)}</h2><p className="mt-2 line-clamp-2 text-sm text-stone-600">{String(g.description)}</p><p className="mt-6 text-xs text-stone-500">Updated {new Date(String(g.updated_at)).toLocaleString()}</p></Link>)}</section>:<section className="rounded-3xl border border-dashed border-stone-300 bg-white p-12 text-center"><h2 className="text-xl font-semibold">Create your first guidebook</h2><p className="mt-2 text-sm text-stone-600">Choose a property to start with synchronized guest essentials.</p></section>}</>}</main>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-2xl border border-stone-200 bg-white p-5"><p className="text-sm text-stone-500">{label}</p><p className="mt-1 text-3xl font-semibold">{value}</p></div>}
