import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const sql=readFileSync("supabase/migrations/20260729030000_bi003_canonical_learning_intelligence.sql","utf8");
describe("BI-003 canonical learning persistence",()=>{it("requires complete outcome-backed lineage and immutable versions",()=>{expect(sql).toContain("learning_lineage_complete");for(const key of["observations","decisions","actions","outcomes","reviews"])expect(sql).toContain(`value->'${key}'`);expect(sql).toContain("create table public.validated_learning_versions");expect(sql).toContain("unique(workspace_id,series_id,version)")});it("uses owner-backed workspace identity and service-only writes",()=>{expect(sql.match(/references public\.owners\(id\)/g)?.length).toBeGreaterThanOrEqual(5);expect(sql).toContain("grant select on public.%I to authenticated");expect(sql).not.toMatch(/grant (insert|update|delete).*authenticated/)});});
