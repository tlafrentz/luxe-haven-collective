import{createLearningLesson,createLearningSubject,createLineageEdge,createMeasurementPlan,createOutcomeReview,reviseLearningLesson,PlatformLearningError,type LearningAssumptionResult,type LearningDomainEvent,type LearningEvidence,type LearningLesson,type LearningLineageEdge,type LearningMeasurementPlan,type LearningOutcomeReview,type LearningSubject,type MeasuredOutcome}from"../domain";
export interface PlatformLearningRepository{createSubject(subject:LearningSubject):Promise<void>;appendLineage(edge:LearningLineageEdge):Promise<void>;appendEvidence(evidence:LearningEvidence):Promise<void>;appendMeasurementPlan(plan:LearningMeasurementPlan):Promise<void>;appendMeasuredOutcome(outcome:MeasuredOutcome):Promise<void>;appendOutcomeReview(review:LearningOutcomeReview):Promise<void>;appendAssumptionResult(result:LearningAssumptionResult):Promise<void>;appendLesson(lesson:LearningLesson):Promise<void>;appendEvents(events:readonly LearningDomainEvent[]):Promise<void>;getSubject(workspaceId:string,id:string):Promise<LearningSubject|null>;getOutcomeReview(workspaceId:string,id:string):Promise<LearningOutcomeReview|null>;getLesson(workspaceId:string,id:string):Promise<LearningLesson|null>;}
export async function createLearningSubjectRecord(repository:PlatformLearningRepository,input:LearningSubject){const result=createLearningSubject(input);await repository.createSubject(result.subject);await repository.appendEvents([result.event]);return result.subject}
export async function createMeasurementPlanRecord(repository:PlatformLearningRepository,input:LearningMeasurementPlan){const plan=createMeasurementPlan(input);await repository.appendMeasurementPlan(plan);return plan}
export async function createLearningLineageRecord(repository:PlatformLearningRepository,input:LearningLineageEdge){const edge=createLineageEdge(input);await repository.appendLineage(edge);return edge}
export async function createLearningEvidenceRecord(repository:PlatformLearningRepository,input:LearningEvidence){if(!input.references.length)throw new PlatformLearningError("learning_evidence_required","Learning Evidence requires at least one source reference.");await repository.appendEvidence(input);return input}
export async function createMeasuredOutcomeRecord(repository:PlatformLearningRepository,input:MeasuredOutcome){if(!input.evidenceId)throw new PlatformLearningError("learning_evidence_required","Measured Outcomes require evidence.");await repository.appendMeasuredOutcome(input);return input}
export async function createOutcomeReviewRecord(repository:PlatformLearningRepository,input:LearningOutcomeReview){const result=createOutcomeReview(input);await repository.appendOutcomeReview(result.review);await repository.appendEvents([result.event]);return result.review}
export async function createAssumptionResultRecord(repository:PlatformLearningRepository,input:LearningAssumptionResult){if(!input.assumption.trim())throw new PlatformLearningError("learning_invalid","An Assumption Result requires a statement.");await repository.appendAssumptionResult(input);return input}
export async function createLessonRecord(repository:PlatformLearningRepository,input:LearningLesson){const result=createLearningLesson(input);await repository.appendLesson(result.lesson);await repository.appendEvents([result.event]);return result.lesson}
export async function updateLessonRecord(repository:PlatformLearningRepository,previous:LearningLesson,input:Omit<LearningLesson,"revision"|"supersedesLessonId">){const result=reviseLearningLesson(previous,input);await repository.appendLesson(result.lesson);await repository.appendEvents([result.event]);return result.lesson}
export const getLearningSubject=(repository:PlatformLearningRepository,workspaceId:string,id:string)=>repository.getSubject(workspaceId,id);
export const getLearningOutcomeReview=(repository:PlatformLearningRepository,workspaceId:string,id:string)=>repository.getOutcomeReview(workspaceId,id);
export const getLearningLesson=(repository:PlatformLearningRepository,workspaceId:string,id:string)=>repository.getLesson(workspaceId,id);
