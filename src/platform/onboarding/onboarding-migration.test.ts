import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260811070000_ca001c_modular_customer_onboarding.sql","utf8");describe("CA-001C persistence",()=>{it("enables RLS and denies anonymous access",()=>{for(const table of["onboarding_templates","onboarding_module_definitions","onboarding_cases","onboarding_plan_templates","onboarding_module_instances","onboarding_submissions","onboarding_documents","onboarding_property_references","onboarding_audit_events","onboarding_notification_attempts"])expect(sql).toContain(`alter table public.${table} enable row level security`);expect(sql).toMatch(/revoke all on[\s\S]+from anon/)});it("prevents browser mutation and active definition rewrites",()=>{expect(sql).toMatch(/revoke insert,update,delete on[\s\S]+from authenticated/);expect(sql).toContain("Active onboarding definitions are immutable")});it("protects document references and allowlists types and size",()=>{expect(sql).toContain("unique(tenant_id,storage_bucket,storage_object_key)");expect(sql).toContain("size_bytes<=26214400");expect(sql).toContain("application/pdf")});it("does not store credentials or billing state",()=>expect(sql).not.toMatch(/password|access_token|secret_key|stripe_price|card_number/))});
