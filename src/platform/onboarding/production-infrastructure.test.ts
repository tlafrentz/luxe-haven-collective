import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const onboarding=readFileSync("supabase/migrations/20260812120000_ca001c_production_operations.sql","utf8"),firstValue=readFileSync("supabase/migrations/20260812121000_ca001d_production_operations.sql","utf8"),admin=readFileSync("supabase/migrations/20260812122000_ca001e_production_operations.sql","utf8");
describe("CA-001C-E production commands",()=>{it("reauthorizes onboarding writes and preserves lifecycle audit",()=>{expect(onboarding).toContain("customer_account_memberships");expect(onboarding).toContain("STALE_ONBOARDING_REVISION");expect(onboarding).toContain("onboarding_case_suspended");expect(onboarding).toContain("onboarding_case_restored");expect(onboarding).toMatch(/revoke all on function[\s\S]+authenticated/)});it("requires active entitlement and canonical evidence for first value",()=>{expect(firstValue).toContain("commercial_entitlements");expect(firstValue).toContain("record_first_value_evidence");expect(firstValue).toContain("STALE_JOURNEY_REVISION");expect(firstValue).toContain("DESTINATION_UNAUTHORIZED");expect(firstValue).not.toMatch(/signed_url|provider_payload|password|access_token/)});it("keeps admin coordination unable to modify commercial or entitlement state",()=>{expect(admin).toContain("authorize_activation_operator");expect(admin).toContain("activation_admin_audit_events");expect(admin).not.toMatch(/update public\.(commercial_agreements|commercial_entitlements|first_value_evidence)/);expect(admin).toMatch(/revoke all on function[\s\S]+authenticated/)})});
