import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
describe("production activation composition",()=>{
 it("uses production Supabase repositories and contains no in-memory fallback",()=>{for(const file of["src/platform/onboarding/infrastructure/production-onboarding.ts","src/platform/onboarding/infrastructure/production-provisioning.ts","src/platform/first-value/infrastructure/production-first-value.ts","src/app/actions/customer-onboarding.ts","src/app/actions/customer-first-value.ts","src/app/actions/admin-activation.ts"]){const source=readFileSync(file,"utf8");expect(source).not.toMatch(/InMemory|no-?op|fixture|placeholder/i);expect(source).not.toContain(".insert(")}});
 it("keeps service transport behind actor-aware operations",()=>{for(const file of["src/app/actions/customer-onboarding.ts","src/app/actions/customer-first-value.ts"]){const source=readFileSync(file,"utf8");expect(source).toContain("requireUser");expect(source).toContain("actorId:user.id")}const admin=readFileSync("src/app/actions/admin-activation.ts","utf8");expect(admin).toContain("requireRole");expect(admin).toContain("actorId:user.id")});
 it("connects admin retry and evaluation to authoritative first-value operations",()=>{const admin=readFileSync("src/app/actions/admin-activation.ts","utf8");expect(admin).toContain("authoritativePorts");expect(admin).toContain("firstValue.process.execute");expect(admin).toContain("firstValue.evaluate.execute");expect(admin).not.toContain("reevaluation_requested")});
});
