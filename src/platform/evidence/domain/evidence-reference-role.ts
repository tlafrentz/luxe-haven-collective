/**
 * Describes the role an observation plays in an evidence interpretation.
 */
export enum EvidenceReferenceRole {
  PRIMARY = "primary",
  SUPPORTING = "supporting",
  CONTEXTUAL = "contextual",
}
