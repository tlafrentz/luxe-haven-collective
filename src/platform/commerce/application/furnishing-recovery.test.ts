import { describe, expect, it } from "vitest";
import { resolveFurnishingRecoveryPolicy } from "./furnishing-recovery";
const base = { entitlement: "active" as const, session: "in_progress" as const, project: "none" as const, activationEnabled: true };
describe("FS-008C recovery policy", () => { it.each([["suspended", "pause"], ["revoked", "terminate"], ["refunded", "terminate"], ["expired", "terminate"]] as const)("handles %s", (entitlement, decision) => expect(resolveFurnishingRecoveryPolicy({ ...base, entitlement }).decision).toBe(decision)); it("resumes reinstated blocked work", () => expect(resolveFurnishingRecoveryPolicy({ ...base, session: "blocked" }).decision).toBe("resume")); it("retries only retryable submitted activation", () => expect(resolveFurnishingRecoveryPolicy({ ...base, session: "submitted", failure: "retryable" }).decision).toBe("retry_activation")); it("requires reconciliation for tampering/incompatible lineage", () => expect(resolveFurnishingRecoveryPolicy({ ...base, failure: "snapshot_integrity" }).decision).toBe("reconcile")); it("preserves active projects", () => expect(resolveFurnishingRecoveryPolicy({ ...base, project: "active", entitlement: "suspended" }).decision).toBe("no_action")); });
