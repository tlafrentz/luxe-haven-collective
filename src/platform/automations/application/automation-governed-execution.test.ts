import { describe, expect, it, vi } from "vitest";
import { createGovernedExecutionService, type GovernedExecutionRepository } from "./automation-governed-execution";
import type { AutomationRunRequest } from "../domain/automation-triggering";

const request: AutomationRunRequest = { id: "request-1", idempotencyKey: "req", tenantId: "tenant-1", scope: { type: "property", propertyIds: ["property-1"] }, automationId: "automation-1", automationDefinitionVersion: 1, triggerId: "trigger-1", triggerKind: "MANUAL", occurrenceId: "occurrence-1", requestedAt: "2026-08-10T12:00:00.000Z", occurredAt: "2026-08-10T12:00:00.000Z", eligibilityPolicyVersion: "v1", approvalClassification: "none", correlationId: "correlation-1", safeTriggerContext: {}, status: "REQUESTED", version: 1 };
const plan = { version: "plan-1", schemaVersion: "au001-execution-plan.v1" as const, definitionVersionId: "definition-version-1", maximumSteps: 5, steps: [{ key: "draft", owningCapability: "execute", commandType: "createDraftPlan", commandContractVersion: "v1", dependencies: [], continuationRule: "all_succeeded" as const, payload: {}, approvalPolicyId: "none", actorPolicyId: "service-policy", retryPolicyId: "retry", timeoutPolicyId: "timeout" }] };
function repository(): GovernedExecutionRepository {
  const runs: any[] = [], steps: any[] = [];
  return { async getRunByRequest(_, id) { return runs.find((run) => run.runRequestId === id) ?? null; }, async getRun(_, id) { return runs.find((run) => run.id === id) ?? null; }, async getSteps(_, id) { return steps.filter((step) => step.runId === id); }, async getApproval() { return null; }, async materialize(input) { runs.push(input.run); steps.push(...input.steps); return { created: true, run: input.run, steps: input.steps }; }, async applyPolicy(input) { const index = runs.findIndex(({ id }) => id === input.run.id); runs[index] = input.run; steps.splice(0, steps.length, ...input.steps); return input.run; }, async transition(input) { return input.run; }, async decideApproval(input) { return { approval: input.approval, run: input.run }; }, async claimStep(input) { const value = steps.find(({ id }) => id === input.stepId); if (!value || value.version !== input.expectedVersion) return null; const claimed = { ...value, status: "leased", leaseOwner: input.workerId, leaseExpiresAt: new Date(Date.parse(input.now) + input.leaseDurationMs).toISOString(), version: value.version + 1 }; steps[steps.indexOf(value)] = claimed; return claimed; }, async recordDispatch(input) { return { run: input.run, step: input.step }; } };
}
function service(input: { enabled?: boolean; kill?: boolean; disposition?: "permitted_without_additional_approval" | "approval_required" | "prohibited" | "insufficient_context" | "policy_unavailable" } = {}) {
  return createGovernedExecutionService({ repository: repository(), definitions: { async getExecution() { return { definitionVersionId: "definition-version-1", active: true, killSwitched: false, plan }; } }, policy: { async evaluate({ run }) { return { id: "decision-1", runId: run.id, disposition: input.disposition ?? "permitted_without_additional_approval", policyVersion: "policy-1", targetContextVersion: "context-1", matchedRules: [], missingFacts: [], safeExplanation: "Allowed by test policy.", evaluatedAt: "2026-08-10T12:00:00.000Z" }; } }, approvalAuthority: { async canApprove() { return true; } }, ports: [], serviceActor: { actorId: "service-1", tenantId: "tenant-1", policyId: "service-policy", active: true, grants: [{ capability: "execute", commandType: "createDraftPlan", propertyIds: ["property-1"] }] }, clock: () => "2026-08-10T12:00:00.000Z", id: vi.fn().mockReturnValueOnce("run-1").mockReturnValue("generated-id"), enabled: () => input.enabled ?? true, killSwitched: () => input.kill ?? false, leaseDurationMs: 60_000 });
}
const owner = { actorId: "owner-1", tenantId: "tenant-1", role: "owner" as const, active: true, propertyIds: [] };
describe("AU-001C application service", () => {
  it("materializes a request idempotently through the repository", async () => { const app = service(); const first = await app.materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); const second = await app.materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); expect(first.ok && first.value.created).toBe(true); expect(second.ok && second.value.created).toBe(false); });
  it("fails closed while disabled or kill-switched", async () => { const disabled = await service({ enabled: false }).materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); const killed = await service({ kill: true }).materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); expect(disabled.ok ? "" : disabled.code).toBe("AUTOMATION_KILL_SWITCHED"); expect(killed.ok ? "" : killed.code).toBe("AUTOMATION_KILL_SWITCHED"); });
  it("fails closed on unavailable policy", async () => { const app = service({ disposition: "policy_unavailable" }); const created = await app.materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); expect(created.ok).toBe(true); if (!created.ok) return; const result = await app.evaluatePolicy({ request, runId: created.value.run.id, expectedVersion: 1, actor: owner }); expect(result.ok ? "" : result.code).toBe("POLICY_EVALUATION_FAILED"); });
  it("creates an approval gate without dispatch", async () => { const app = service({ disposition: "approval_required" }); const created = await app.materialize({ request, actor: owner, serviceActorPolicyId: "service-policy" }); if (!created.ok) throw new Error(); const result = await app.evaluatePolicy({ request, runId: created.value.run.id, expectedVersion: 1, actor: owner }); expect(result.ok && result.value.status).toBe("awaiting_approval"); });
});
