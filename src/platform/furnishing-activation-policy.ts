export type FurnishingGlobalState = "disabled" | "internal" | "limited" | "enabled" | "paused";
export type FurnishingActivationContext = Readonly<{ globalKillSwitch:boolean; globalState:FurnishingGlobalState; workspaceKillSwitch:boolean; workspaceEnabled:boolean; cohortEligible:boolean; cohortExpired?:boolean; capabilityEnabled:boolean; offerActive?:boolean; catalogAvailable?:boolean; configurationValid:boolean; policyVersion:string; evaluatedAt?:string }>;
export function resolveFurnishingActivation(context:FurnishingActivationContext){ const evaluatedAt=context.evaluatedAt??new Date().toISOString(); let reason="enabled"; if(!context.configurationValid)reason="configuration_invalid";else if(context.globalKillSwitch)reason="killed_globally";else if(context.globalState==="disabled")reason="disabled_globally";else if(context.workspaceKillSwitch)reason="killed_globally";else if(!context.workspaceEnabled)reason="workspace_not_enabled";else if(context.cohortExpired)reason="cohort_expired";else if(!context.cohortEligible)reason="not_in_cohort";else if(!context.capabilityEnabled)reason="capability_disabled";else if(context.offerActive===false)reason="offer_inactive";else if(context.catalogAvailable===false)reason="catalog_unavailable"; return Object.freeze({allowed:reason==="enabled",reason,policyVersion:context.policyVersion,evaluatedAt}); }
