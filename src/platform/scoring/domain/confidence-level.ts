/**
 * Human-readable interpretation of a confidence score.
 */
export enum ConfidenceLevel {
  VERY_LOW = "very-low",
  LOW = "low",
  MODERATE = "moderate",
  HIGH = "high",
  VERY_HIGH = "very-high",
}
