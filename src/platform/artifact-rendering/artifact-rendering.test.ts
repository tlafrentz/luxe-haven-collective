import{describe,expect,it}from"vitest";import{ArtifactRenderingEngine,type ArtifactRenderer,type PublishedArtifactEnvelope}from".";
const artifact:PublishedArtifactEnvelope<{title:string}>={artifactType:"guidebook",artifactVersion:"guidebook-publication-snapshot.v1",rendererVersion:"guidebook-web-renderer.v1",publishedAt:"2026-07-26T12:00:00Z",version:2,payload:{title:"Guest Guide"}};
describe("Artifact Rendering Engine",()=>{it("renders only through the matching versioned adapter",()=>{const renderer:ArtifactRenderer<{title:string},{heading:string}>={artifactType:"guidebook",rendererVersion:"guidebook-web-renderer.v1",render:value=>({heading:value.payload.title})},engine=new ArtifactRenderingEngine().register(renderer);expect(engine.render(artifact)).toEqual({heading:"Guest Guide"})});it("rejects unsupported artifacts",()=>expect(()=>new ArtifactRenderingEngine().render(artifact)).toThrow("artifact_renderer_unavailable"));it("rejects implicit renderer upgrades",()=>expect(()=>new ArtifactRenderingEngine().register({artifactType:"guidebook",rendererVersion:"guidebook-web-renderer.v2",render:value=>value}).render(artifact)).toThrow("artifact_renderer_version_mismatch"));});
