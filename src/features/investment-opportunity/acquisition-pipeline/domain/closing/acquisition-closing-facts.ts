import type { AcquisitionMoney } from "../offers/acquisition-offer-terms";
import { AcquisitionDomainError } from "../errors";
export type PurchaseAcquisitionClosingFacts = Readonly<{ route: "purchase"; closedAt: Date; finalPurchasePrice: AcquisitionMoney; finalClosingCosts?: AcquisitionMoney; finalSellerConcessions?: AcquisitionMoney; financingType: "cash" | "financed"; possessionAt?: Date; externalClosingReference?: string }>;
export type RentalArbitrageAcquisitionClosingFacts = Readonly<{ route: "rental-arbitrage"; agreementExecutedAt: Date; commencementAt: Date; possessionAt?: Date; finalMonthlyRent: AcquisitionMoney; finalSecurityDeposit?: AcquisitionMoney; operatingPermissionStatus: "explicitly-authorized" | "unclear" | "not-authorized"; externalAgreementReference?: string }>;
export type AcquisitionClosingFacts = PurchaseAcquisitionClosingFacts | RentalArbitrageAcquisitionClosingFacts;
export function validateClosingFacts(facts: AcquisitionClosingFacts): void { const date = facts.route === "purchase" ? facts.closedAt : facts.agreementExecutedAt; const amount = facts.route === "purchase" ? facts.finalPurchasePrice : facts.finalMonthlyRent; if (Number.isNaN(date.getTime()) || amount.amount <= 0 || amount.currency !== "USD") throw new AcquisitionDomainError("ACQUISITION_CLOSING_FACTS_INVALID"); if (facts.route === "rental-arbitrage" && facts.commencementAt < facts.agreementExecutedAt) throw new AcquisitionDomainError("ACQUISITION_CLOSING_FACTS_INVALID"); }
