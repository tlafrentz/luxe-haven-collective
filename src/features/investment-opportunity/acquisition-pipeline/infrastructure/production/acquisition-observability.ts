export interface AcquisitionObservability { log(event: string, fields: Readonly<Record<string, string | number | boolean | undefined>>): void; increment(metric: string, value?: number): void; time<T>(metric: string, operation: () => Promise<T>): Promise<T>; }
export const noopAcquisitionObservability: AcquisitionObservability = { log: () => undefined, increment: () => undefined, time: <T>(_metric: string, operation: () => Promise<T>) => operation() };
export function createConsoleAcquisitionObservability(): AcquisitionObservability { return { log: (event, fields) => console.info(JSON.stringify({ event, ...fields })), increment: (metric, value = 1) => console.info(JSON.stringify({ event: "acquisition.metric", metric, value })), time: async (metric, operation) => { const started = Date.now(); try { return await operation(); } finally { console.info(JSON.stringify({ event: "acquisition.timer", metric, durationMs: Date.now() - started })); } } }; }
