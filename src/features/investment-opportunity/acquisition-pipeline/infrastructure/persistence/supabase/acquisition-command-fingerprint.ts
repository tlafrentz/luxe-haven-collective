export function createAcquisitionCommandFingerprint(input: Readonly<{ commandType: string; ownerId: string; pipelineId?: string; opportunityId?: string; canonicalPayload: unknown }>): string { const canonical = stable(input.canonicalPayload); let hash = 2166136261; for (const char of `v1|${input.commandType}|${input.ownerId}|${input.pipelineId ?? ""}|${input.opportunityId ?? ""}|${canonical}`) { hash ^= char.charCodeAt(0); hash = Math.imul(hash, 16777619); } return `v1:${(hash >>> 0).toString(16).padStart(8, "0")}`; }
function stable(value: unknown): string { if (value === null || typeof value !== "object") return JSON.stringify(value); if (Array.isArray(value)) return `[${value.map(stable).join(",")}]`; return `{${Object.keys(value as Record<string, unknown>).sort().map(key => `${JSON.stringify(key)}:${stable((value as Record<string, unknown>)[key])}`).join(",")}}`; }
