import type { InvestmentOpportunity } from "../../domain";
import type { OpportunityComparisonIncompatibilityReason } from "./comparison-types";

export function assessComparisonCompatibility(opportunities: readonly (InvestmentOpportunity | null)[]) { const reasons: OpportunityComparisonIncompatibilityReason[] = []; if (opportunities.length < 2) reasons.push("TOO_FEW_OPPORTUNITIES"); if (opportunities.length > 4) reasons.push("TOO_MANY_OPPORTUNITIES"); if (opportunities.some(value => !value)) reasons.push("OPPORTUNITY_NOT_ACCESSIBLE"); const present = opportunities.filter((value): value is InvestmentOpportunity => Boolean(value)); if (new Set(present.map(value => value.props.route)).size > 1) reasons.push("PURCHASE_RENTAL_ROUTE_MISMATCH"); if (present.some(value => value.props.archivedAt)) reasons.push("ARCHIVED_OPPORTUNITY"); if (present.some(value => !value.props.currentAnalysisId || !value.props.analyses.some(analysis => analysis.id.value === value.props.currentAnalysisId?.value))) reasons.push("MISSING_CURRENT_ANALYSIS"); if (present.some(value => value.props.analyses.find(analysis => analysis.id.value === value.props.currentAnalysisId?.value)?.props.resultSnapshot.schemaVersion !== "1")) reasons.push("UNSUPPORTED_SNAPSHOT_VERSION"); return Object.freeze({ compatible: reasons.length === 0, reasons: Object.freeze([...new Set(reasons)]) }); }
