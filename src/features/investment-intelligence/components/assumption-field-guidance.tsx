import { getInvestmentAssumptionGuidance, type InvestmentAssumptionGuidanceId } from "../application/assumptions";

export function AssumptionFieldGuidance({ id }: { id: InvestmentAssumptionGuidanceId }) { const guidance = getInvestmentAssumptionGuidance(id); return <div className="mt-1.5 text-xs leading-5 text-neutral-500"><p>{guidance.shortDescription}</p>{guidance.displayPriority !== "tooltip" ? <details className="mt-1"><summary className="cursor-pointer font-medium text-neutral-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2">What to include</summary><p className="mt-2">{guidance.description}</p>{guidance.excludes?.length ? <p className="mt-1"><span className="font-medium">Do not include:</span> {guidance.excludes.join(", ")}.</p> : null}<p className="mt-1"><span className="font-medium">Source:</span> {guidance.sourceDescription}</p></details> : null}</div>; }
