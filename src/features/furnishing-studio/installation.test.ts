import{describe,expect,it}from"vitest";import{canAuthorizeLaunch,installationTaskStatus,installableQuantity,launchReadiness,roomAcceptance,scheduleConflict}from"./installation";
describe("FS-007 installation domain",()=>{it("uses only accepted procurement quantities",()=>expect(installableQuantity({acceptedProcurementQuantity:6,alreadyInstalled:2,damaged:1,reservedElsewhere:1})).toBe(2));it("blocks tasks until inventory dependencies and site are ready",()=>expect(installationTaskStatus({dependenciesComplete:true,siteReady:false,availableQuantity:1,installedQuantity:0,acceptedQuantity:0,requiredQuantity:1,deferred:false})).toBe("blocked"));it("computes explainable readiness without authorizing launch",()=>expect(launchReadiness([{id:"power",category:"utilities",required:true,status:"passed",weight:1},{id:"wifi",category:"technology",required:true,status:"failed",weight:1}],0)).toEqual({percent:50,blockers:["wifi"],readyForHumanAuthorization:false}));it("requires task safety evidence and punch-list completion for room acceptance",()=>expect(roomAcceptance({requiredTasks:3,acceptedTasks:3,blockingPunchItems:0,safetyPassed:true,evidenceComplete:true}).accepted).toBe(true));it("requires explicit authority for launch",()=>expect(canAuthorizeLaunch({assessmentCurrent:true,readinessPercent:100,blockers:0,roomsAccepted:3,roomsRequired:3,procurementClosed:true,authorityGranted:false}).authorized).toBe(false));it("detects overlapping crew assignments",()=>expect(scheduleConflict([{id:"a",crewIds:["c"],startsAt:"2026-08-07T10:00:00Z",endsAt:"2026-08-07T12:00:00Z"},{id:"b",crewIds:["c"],startsAt:"2026-08-07T11:00:00Z",endsAt:"2026-08-07T13:00:00Z"}])).toHaveLength(1))});
