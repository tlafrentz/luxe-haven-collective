import{describe,expect,it}from"vitest";import{classifyDelivery,correctionRequiresReapproval,propertyCompletion,reconcileTrackingQuantities,roomCompletion,tvMountCompletion}from"./delivery-installation";
describe("FS-UX-007 tracking rules",()=>{it("reconciles every quantity boundary",()=>expect(reconcileTrackingQuantities({planned:2,ordered:2,notOrdered:0,received:1,inTransit:1,cancelled:0,missing:0,installed:1})).toEqual({orderReconciled:true,receiptReconciled:true,installationReconciled:true,complete:false}));it("distinguishes carrier delivery from receipt",()=>expect(classifyDelivery({carrierDelivered:true,physicallyReceived:false})).toBe("carrier_reported_delivered"));it("blocks room completion on exceptions",()=>expect(roomCompletion({requiredPlanned:1,requiredReceived:1,requiredInstalled:1,requiredInspected:1,blockingExceptions:1,quantityReconciled:true})).toBe("blocked"));it("requires rule-based property completion",()=>expect(propertyCompletion(["complete","complete"],true,0,true)).toBe(true));it("keeps TVs and mounts separate",()=>expect(tvMountCompletion({tvReceived:true,mountRequired:true,mountReceived:false,compatible:true,tvInstalled:true,mountInstalled:false,blockingException:false})).toBe(false));it("requires reapproval for material corrections",()=>expect(correctionRequiresReapproval(["caption","receipt"])).toBe(true))});
