import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260806031000_lhc_gs_003_content_library.sql","utf8");describe("LHC-GS-003 persistence",()=>{it("stores identity, approved version, replacement, usage, review, archive, and bulk commands",()=>{for(const value of["content_record_type","approved_version_id","replacement_content_id","guidebook_content_usage","guidebook_content_reviews","guidebook_content_archive_events","guidebook_content_bulk_commands"])expect(sql).toContain(value)});it("enforces immutable approved content and idempotent bulk operations",()=>{expect(sql).toContain("content_version_immutable");expect(sql).toContain("idempotency_key");expect(sql).toContain("unique(workspace_id,idempotency_key)")});it("enforces workspace RLS and rejects anonymous access",()=>{expect(sql).toContain("enable row level security");expect(sql).toContain("from anon")})});
