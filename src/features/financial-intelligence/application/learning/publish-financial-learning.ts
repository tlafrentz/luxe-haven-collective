import type{FinancialLearningRepository,FinancialLesson,FinancialOutcomeReview}from"./contracts";import{FinancialLearningError}from"./get-financial-learning";
export async function appendFinancialReview(repository:FinancialLearningRepository,review:FinancialOutcomeReview,input:Readonly<{idempotencyKey:string;canReview:boolean}>){if(!input.canReview)throw new FinancialLearningError("permission","Planning Review permission is required.");if(!review.immutable)throw new FinancialLearningError("evidence","Financial reviews must preserve immutable expectation lineage.");return repository.appendReview(Object.freeze({...review,metrics:Object.freeze([...review.metrics]),assumptions:Object.freeze([...review.assumptions]),evidenceIds:Object.freeze([...review.evidenceIds])}),input.idempotencyKey)}
export async function publishFinancialLearning(repository:FinancialLearningRepository,lesson:FinancialLesson,input:Readonly<{idempotencyKey:string;canPublish:boolean}>){if(!input.canPublish)throw new FinancialLearningError("permission","Financial Learning permission is required.");if(!lesson.derivedFromReviewIds.length||!lesson.evidenceIds.length)throw new FinancialLearningError("evidence","Published lessons require outcome review lineage and evidence.");return repository.publishLesson(Object.freeze({...lesson,derivedFromReviewIds:Object.freeze([...lesson.derivedFromReviewIds]),evidenceIds:Object.freeze([...lesson.evidenceIds])}),input.idempotencyKey)}
