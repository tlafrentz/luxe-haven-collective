import Link from "next/link";
import type { CashFlowLine, CashFlowLiquidityView as View } from "../application";
import { CashDataAcquisition } from "./cash-data-acquisition";

export function CashFlowLiquidityPage({ view }: { view: View }) {
  if (view.state === "empty") return <CashFlowEmpty workspaceId={view.identity.workspaceId} />;
  return <main className="mx-auto max-w-[1500px] space-y-8 px-4 py-8 sm:px-6 lg:px-8">
    <Header view={view} /><div className="flex items-center justify-between gap-4 rounded-2xl border border-stone-200 bg-white p-4"><p className="text-sm text-stone-600">Add another cash account or import transaction history.</p><CashDataAcquisition workspaceId={view.identity.workspaceId}/></div>
    {view.permissionLimited ? <Notice title="Property Cash Flow">You can view authorized cash summaries, but restricted workspace balances, accounts, reserves, or movement detail are omitted at the application boundary.</Notice> : null}
    {view.state === "partial" || view.state === "balances-only" || view.state === "transactions-only" ? <Notice title="Liquidity analysis is partially available">{view.evidence.gaps.join(" ")}</Notice> : null}
    {view.state === "degraded" ? <Notice title="Cash analysis may be incomplete">A limiting cash source is stale. Last-known values are qualified below.</Notice> : null}
    {view.state === "unreconciled" ? <Notice title="Cash flow does not fully reconcile">{view.statement.reconciliation.explanation}</Notice> : null}
    <Condition view={view} />
    <Position view={view} />
    <div className="grid gap-8 xl:grid-cols-2"><Statement view={view} /><Drivers view={view} /></div>
    <div className="grid gap-8 xl:grid-cols-2"><Accounts view={view} /><Obligations view={view} /></div>
    <div className="grid gap-8 xl:grid-cols-2"><Properties view={view} /><Reserves view={view} /></div>
    <div className="grid gap-8 xl:grid-cols-2"><BurnRunway view={view} /><Outlook view={view} /></div>
    <Trends view={view} />
    <Attention view={view} />
    <Evidence view={view} />
  </main>;
}
function Header({view}:{view:View}) { return <header className="rounded-[2rem] bg-[#101416] p-6 text-white sm:p-8">
  <nav aria-label="Financial Intelligence"><ol className="flex flex-wrap gap-2 text-xs font-semibold"><li><Link className="rounded-full border border-white/20 px-3 py-2 text-stone-300" href="/dashboard/financial">Overview</Link></li><li><Link className="rounded-full border border-white/20 px-3 py-2 text-stone-300" href="/dashboard/financial/profitability">Profitability</Link></li><li><span aria-current="page" className="inline-flex rounded-full bg-white px-3 py-2 text-stone-950">Cash Flow</span></li><li><Link className="rounded-full border border-white/20 px-3 py-2 text-stone-300" href="/dashboard/financial/planning">Planning</Link></li></ol></nav>
  <p className="mt-6 text-xs font-semibold uppercase tracking-[.18em] text-teal-200">Understand · Financial Intelligence</p>
  <div className="mt-3 flex flex-col gap-6 xl:flex-row xl:items-end xl:justify-between"><div><h1 className="text-3xl font-semibold sm:text-4xl">Cash Flow &amp; Liquidity</h1><p className="mt-3 text-lg text-stone-200">{view.scope.label}</p><p className="mt-1 text-sm text-stone-400">{view.scope.propertyCount} included properties · {range(view.period.from,view.period.to)}</p></div><dl className="grid grid-cols-2 gap-5 sm:grid-cols-3"><Fact label="Currency" value={view.reportingCurrency}/><Fact label="Accounts" value={`${view.accounts.length} visible`}/><Fact label="Comparison" value={view.comparison?.available?title(view.comparison.type):"Unavailable"}/><Fact label="Confidence" value={title(view.confidence)}/><Fact label="Freshness" value={title(view.freshness)}/><Fact label="Evaluated" value={dateTime(view.evaluatedAt)}/></dl></div>
  <form aria-label="Cash Flow reporting context" className="mt-7 grid gap-3 border-t border-white/10 pt-6 sm:grid-cols-2 lg:grid-cols-4"><Control label="Scope" name="scope" options={[["workspace","Full Workspace"],["selected-properties","Selected Properties"],["single-property","Single Property"]]}/><Control label="Period" name="period" options={[["this-month","This Month"],["last-month","Last Month"],["qtd","Quarter to Date"],["ytd","Year to Date"],["12m","Trailing 12 Months"]]}/><Control label="Comparison" name="comparison" options={[["previous-period","Previous Period"],["previous-year","Previous Year"],["forecast","Forecast"],["none","No Comparison"]]}/><Control label="Obligations" name="horizon" options={[["7","Next 7 Days"],["30","Next 30 Days"],["60","Next 60 Days"],["90","Next 90 Days"]]}/><button className="min-h-11 rounded-full bg-white px-5 text-sm font-semibold text-stone-950 sm:col-span-2 sm:justify-self-start lg:col-span-4">Update cash view</button></form>
  </header>; }
function Condition({view}:{view:View}) { return <section className="rounded-[2rem] border border-stone-200 bg-white p-6 sm:p-8" aria-labelledby="liquidity-condition"><Pill>{title(view.condition.status)}</Pill><h2 id="liquidity-condition" className="mt-4 text-3xl font-semibold">Liquidity Condition</h2><p className="mt-3 max-w-3xl text-lg text-stone-700">{view.condition.summary}</p><div className="mt-6 grid gap-4 md:grid-cols-2"><List title="Positive evidence" items={view.condition.positiveDrivers}/><List title="Limitations" items={view.condition.limitingConditions}/></div><p className="mt-5 text-xs text-stone-500">{title(view.condition.confidence)} confidence · Operational interpretation, not a legal, credit, or accounting opinion.</p></section>; }
function Position({view}:{view:View}) { const p=view.position, items=[["Total Cash",p.totalCash],["Available Cash",p.availableCash],["Restricted / Reserved",p.restrictedCash],["Committed Cash",p.committedCash],["Net Cash Movement",p.netCashMovement],["Opening / Closing",p.openingCash&&p.closingCash?`${p.openingCash.format()} → ${p.closingCash.format()}`:null]] as const; return <Section id="cash-position" title="Cash position" description="Measured balances from included, authorized cash accounts; never inferred from profit."><dl className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(([label,value])=><article key={label} className="rounded-2xl border border-stone-200 p-5"><Fact label={label} value={typeof value==="string"?value:value?.format()??"Unavailable"}/><p className="mt-3 text-xs text-stone-500">As of {p.asOf?date(p.asOf):"unavailable"} · {title(p.confidence)} confidence</p></article>)}</dl></Section>; }
function Statement({view}:{view:View}) { const sections=[view.statement.operatingActivities,view.statement.investingActivities,view.statement.financingActivities,view.statement.otherAdjustments]; return <Card id="cash-statement" title="Cash Flow Statement" description="Economic cash activity after matched internal-transfer elimination.">{sections.map(section=><div key={section.activity} className="border-b border-stone-100 py-4"><div className="flex justify-between gap-4"><strong>{title(section.activity)} Activities</strong><strong>{section.net.format()}</strong></div>{section.lines.length?<ul className="mt-3 space-y-2">{section.lines.map(line=><Line key={line.id} line={line}/>)}</ul>:<p className="mt-2 text-xs text-stone-500">Detailed lines unavailable or restricted.</p>}</div>)}<dl className="mt-5 grid grid-cols-2 gap-4"><Fact label="Net change in cash" value={view.statement.netCashMovement.format()}/><Fact label="Reconciliation" value={title(view.statement.reconciliation.status)}/><Fact label="Opening cash" value={view.statement.openingCash?.format()??"Unavailable"}/><Fact label="Closing cash" value={view.statement.closingCash?.format()??"Unavailable"}/></dl><p className="mt-4 text-xs text-stone-500">{view.statement.internalTransfersEliminated} matched transfer pairs eliminated. {view.statement.reconciliation.explanation}</p></Card>; }
function Drivers({view}:{view:View}) { return <Card id="cash-drivers" title="Cash movement drivers" description="Largest evidence-backed sources and uses of cash; transfer activity is not treated as economic movement."><DriverList title="Largest inflows" items={view.drivers.inflows}/><DriverList title="Largest outflows" items={view.drivers.outflows}/><DriverList title="Nonrecurring events" items={view.drivers.nonrecurring}/></Card>; }
function Accounts({view}:{view:View}) { return <Card id="cash-accounts" title="Cash by account" description="Account identifiers are omitted; balances and source status appear only when permitted.">{view.accounts.length?<ul className="divide-y divide-stone-100">{view.accounts.map(item=><li key={item.id} className="py-4"><div className="flex justify-between gap-4"><span><strong>{item.label}</strong><small className="block text-stone-500">{title(item.type)} · {title(item.restriction)}</small></span><span className="text-right"><strong>{item.balance?.format()??"Balance unavailable"}</strong><small className="block text-stone-500">{title(item.status)} · {title(item.reconciliation)}</small></span></div></li>)}</ul>:<Empty>Cash account detail is unavailable or restricted.</Empty>}</Card>; }
function Obligations({view}:{view:View}) { const o=view.obligations; return <Card id="cash-obligations" title={`Upcoming obligations · ${o.horizonDays} days`} description="Missing obligation evidence is never interpreted as no obligations.">{o.items.length?<ul className="divide-y divide-stone-100">{o.items.map(item=><li key={item.id} className="flex justify-between gap-4 py-4"><span><strong>{item.description}</strong><small className="block text-stone-500">{title(item.category)} · due {date(item.dueDate)} · {title(item.status)}</small></span><strong>{item.amount.format()}</strong></li>)}</ul>:<Empty>{o.sourceAvailable?"No known obligations are present in the available source for this horizon.":"Obligation coverage unavailable. Known future payments have not been configured or imported."}</Empty>}<dl className="mt-5 grid grid-cols-2 gap-4"><Fact label="Known total" value={o.totalKnown?.format()??"Unavailable"}/><Fact label="Available-cash coverage" value={o.availableCashCoverage===null?"Unavailable":`${o.availableCashCoverage.toFixed(1)}×`}/><Fact label="Funding gap" value={o.fundingGap?.format()??"Unavailable"}/><Fact label="Evidence coverage" value={percent(o.coverage)}/></dl></Card>; }
function Properties({view}:{view:View}) { return <Card id="property-cash" title="Property cash contribution" description="Authorized, explicitly attributed cash activity reconciles to scope; shared items remain unallocated.">{view.propertyContribution.length?<ul className="divide-y divide-stone-100">{view.propertyContribution.map(item=><li key={item.propertyId} className="py-4"><h3 className="font-semibold">{item.label}</h3><dl className="mt-3 grid grid-cols-2 gap-3"><Fact label="Operating cash flow" value={item.operatingCashFlow.format()}/><Fact label="Capital cash flow" value={item.investingCashFlow.format()}/><Fact label="Financing cash flow" value={item.financingCashFlow.format()}/><Fact label="Net contribution" value={item.netCashContribution.format()}/></dl></li>)}</ul>:<Empty>No attributable property cash activity.</Empty>}<p className="mt-4 text-xs text-stone-500">Unallocated cash activity: {view.unallocatedCashActivity.format()}</p></Card>; }
function Reserves({view}:{view:View}) { return <Card id="cash-reserves" title="Reserve coverage" description="Targets come only from configured workspace or property reserve policies.">{view.reserves.items.length?<ul className="divide-y divide-stone-100">{view.reserves.items.map(item=><li key={item.policyId} className="py-4"><div className="flex justify-between"><strong>{title(item.type)}</strong><Pill>{title(item.status)}</Pill></div><dl className="mt-3 grid grid-cols-2 gap-3"><Fact label="Balance" value={item.currentBalance?.format()??"Unavailable"}/><Fact label="Target" value={item.target.format()}/><Fact label="Coverage" value={item.coveragePercentage===null?"Unavailable":percent(item.coveragePercentage)}/><Fact label="Months" value={item.monthsOfCoverage===null?"Unavailable":item.monthsOfCoverage.toFixed(1)}/></dl></li>)}</ul>:<Empty>Reserve target not configured. Current reserve balances may be visible, but target coverage cannot be evaluated.</Empty>}</Card>; }
function BurnRunway({view}:{view:View}) { return <Card id="cash-runway" title="Burn rate & runway" description="Conditional estimates disclose their lookback, assumptions, and exclusions."><dl className="grid grid-cols-2 gap-4"><Fact label="Gross cash burn" value={view.burnRate.grossCashBurn?.format()??"Unavailable"}/><Fact label="Operating cash burn" value={view.burnRate.applicable?view.burnRate.operatingCashBurn?.format()??"Unavailable":"No operating cash burn"}/><Fact label="Estimated runway" value={view.runway.status==="available"?`${view.runway.months?.toFixed(1)} months`:title(view.runway.status)}/><Fact label="Basis" value={view.runway.basis}/></dl><p className="mt-5 text-xs text-stone-500">Assumptions: {view.runway.assumptions.join(" ")} Excludes: {view.runway.exclusions.join(", ")}.</p></Card>; }
function Outlook({view}:{view:View}) { const o=view.outlook; return <Card id="cash-outlook" title={`${o.horizonDays}-Day liquidity outlook`} description="A bounded outlook from current cash and known scheduled activity—not the full forecast engine."><Pill>{title(o.status)}</Pill><dl className="mt-5 grid grid-cols-2 gap-4"><Fact label="Opening available cash" value={o.openingAvailableCash?.format()??"Unavailable"}/><Fact label="Scheduled inflows" value={o.scheduledInflows?.format()??"Unavailable"}/><Fact label="Scheduled outflows" value={o.scheduledOutflows?.format()??"Unavailable"}/><Fact label="Projected closing cash" value={o.projectedClosingAvailableCash?.format()??"Unavailable"}/></dl><p className="mt-5 text-xs text-stone-500">{title(o.qualification)} · {title(o.confidence)} confidence · {o.assumptions.join(" ")}</p></Card>; }
function Trends({view}:{view:View}) { return <Section id="cash-trends" title="Cash-flow trends" description="Operating, investing, financing, and balance movement remain separate."><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{view.trends.map(item=><article key={item.metric} className="rounded-2xl border border-stone-200 bg-white p-5"><Fact label={title(item.metric)} value={item.current?.format()??"Unavailable"}/><p className="mt-3 text-xs text-stone-500">{title(item.classification)} · {item.variance?.format()??"comparison unavailable"}</p></article>)}</div></Section>; }
function Attention({view}:{view:View}) { return <Section id="cash-attention" title="Liquidity attention" description="Descriptive observations for inspection, not financing or capital recommendations.">{view.attention.length?<ul className="grid gap-4 md:grid-cols-2">{view.attention.map(item=><li key={item.id} className="rounded-2xl border border-amber-200 bg-amber-50 p-5"><Pill>{title(item.type)}</Pill><h3 className="mt-3 font-semibold">{item.subject}</h3><p className="mt-2 text-sm">{item.condition}</p><p className="mt-2 text-sm text-stone-600">{item.whyItMatters}</p><p className="mt-3 text-xs">{title(item.confidence)} confidence</p></li>)}</ul>:<Empty>No material liquidity attention items under the current evidence.</Empty>}</Section>; }
function Evidence({view}:{view:View}) { const e=view.evidence; const values=[["Cash accounts",e.accountCoverage],["Transactions",e.transactionCoverage],["Transfer matching",e.transferMatchCoverage],["Obligations",e.obligationCoverage],["Property attribution",e.propertyAttribution]] as const; return <Section id="cash-evidence" title="Evidence & reconciliation" description="Coverage, matching, reconciliation, confidence, and freshness qualify every liquidity conclusion."><div className="grid gap-6 lg:grid-cols-[1fr_2fr]"><div><p className="text-3xl font-semibold">{title(e.confidence)}</p><p className="text-sm text-stone-600">Cash confidence</p><p className="mt-4 text-sm">Reconciliation: <strong>{title(e.reconciliation)}</strong></p><p className="mt-2 text-sm">History: <strong>{e.historyMonths} months</strong></p></div><dl className="grid grid-cols-2 gap-4">{values.map(([label,value])=><Fact key={label} label={label} value={percent(value)}/>)}</dl></div>{e.gaps.length?<List title="Evidence limitations" items={e.gaps}/>:null}</Section>; }

export function CashFlowEmpty({workspaceId="00000000-0000-4000-8000-000000000000"}:{workspaceId?:string}){return <main className="mx-auto max-w-3xl px-4 py-10 sm:py-16"><section role="status" className="rounded-[2rem] border border-stone-200 bg-white p-6 text-center sm:p-8"><p className="text-xs font-semibold uppercase tracking-wide text-emerald-800">Financial Intelligence</p><h1 className="mt-3 text-3xl font-semibold">Cash position unavailable</h1><p className="mx-auto mt-4 max-w-lg text-sm leading-6 text-stone-600">Enter a starting cash balance or import financial data to begin liquidity analysis. Bank sync will appear only when a supported provider is enabled.</p><div className="mx-auto mt-7 flex justify-center"><CashDataAcquisition workspaceId={workspaceId}/></div><p className="mt-7 text-sm text-stone-600">Already connected something? <Link href="/dashboard/workspace/connected-systems?returnTo=%2Fdashboard%2Fobserve%2Ffinancial%2Fcash-flow" className="font-semibold text-emerald-800 underline">Review connected systems</Link></p></section></main>}
export function CashFlowErrorView({code}:{code:string;message:string}){const detail=code==="currency"?"Incompatible currencies. Included cash accounts need one approved reporting currency.":"Your data is safe. Try again, or inspect the financial sources used for this view.";return <main className="mx-auto max-w-3xl px-4 py-16"><section role="alert" aria-live="assertive" className="rounded-[2rem] border border-rose-200 bg-white p-8 text-center"><p className="text-xs font-semibold uppercase tracking-wide text-rose-700">{title(code)} error</p><h1 className="mt-3 text-3xl font-semibold">We couldn&apos;t load your cash flow</h1><p className="mt-3 text-sm text-stone-600">{detail}</p><p className="mt-1 text-sm text-stone-600">Your data is safe.</p><Link href="/dashboard/workspace/connected-systems" className="mt-6 inline-flex rounded-full border px-5 py-3 text-sm font-semibold">View data sources</Link></section></main>}
export function CashFlowSkeleton(){return <main aria-label="Loading Cash Flow" aria-busy="true" className="mx-auto max-w-[1500px] animate-pulse space-y-7 px-4 py-8 motion-reduce:animate-none"><div className="h-72 rounded-[2rem] bg-stone-200"/><div className="h-48 rounded-[2rem] bg-stone-200"/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:6},(_,i)=><div key={i} className="h-32 rounded-2xl bg-stone-200"/>)}</div><div className="grid gap-8 xl:grid-cols-2"><div className="h-96 rounded-[2rem] bg-stone-200"/><div className="h-96 rounded-[2rem] bg-stone-200"/></div></main>}
function Section({id,title:label,description,children}:{id:string;title:string;description:string;children:React.ReactNode}){return <section id={id} aria-labelledby={`${id}-heading`}><Heading id={`${id}-heading`} title={label} description={description}/><div className="mt-5">{children}</div></section>}
function Card(props:{id:string;title:string;description:string;children:React.ReactNode}){return <section id={props.id} aria-labelledby={`${props.id}-heading`} className="rounded-[2rem] border border-stone-200 bg-white p-6"><Heading id={`${props.id}-heading`} title={props.title} description={props.description}/><div className="mt-5">{props.children}</div></section>}
function Heading({id,title:label,description}:{id:string;title:string;description:string}){return <div><h2 id={id} className="text-2xl font-semibold tracking-tight">{label}</h2><p className="mt-1 max-w-3xl text-sm leading-6 text-stone-600">{description}</p></div>}
function Fact({label,value}:{label:string;value:string}){return <div><dt className="text-[11px] font-semibold uppercase tracking-[.1em] text-stone-500">{label}</dt><dd className="mt-1 text-sm font-semibold">{value}</dd></div>}
function Line({line}:{line:CashFlowLine}){return <li className="flex justify-between gap-3 text-sm"><span>{title(line.label)} <small className="text-stone-500">({title(line.direction)}, {title(line.qualification)})</small></span><strong>{line.direction==="outflow"?"−":""}{line.amount.format()}</strong></li>}
function DriverList({title:label,items}:{title:string;items:View["drivers"]["inflows"]}){return <div className="mt-5"><h3 className="font-semibold">{label}</h3>{items.length?<ul className="mt-2 divide-y divide-stone-100">{items.slice(0,5).map(item=><li key={item.id} className="flex justify-between py-3 text-sm"><span>{title(item.label)}<small className="block text-stone-500">{title(item.activity)} · {title(item.recurring)}</small></span><strong>{item.amount.format()}</strong></li>)}</ul>:<Empty>No authorized driver detail.</Empty>}</div>}
function List({title:label,items}:{title:string;items:readonly string[]}){return <div className="mt-4 rounded-xl bg-stone-50 p-4"><h3 className="text-sm font-semibold">{label}</h3>{items.length?<ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-stone-600">{items.map(item=><li key={item}>{item}</li>)}</ul>:<p className="mt-2 text-sm text-stone-500">None established.</p>}</div>}
function Empty({children}:{children:React.ReactNode}){return <p className="mt-4 rounded-xl bg-stone-50 p-4 text-sm text-stone-600">{children}</p>}
function Notice({title:label,children}:{title:string;children:React.ReactNode}){return <aside role="status" className="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-950"><strong>{label}.</strong> {children}</aside>}
function Pill({children}:{children:React.ReactNode}){return <span className="inline-flex rounded-full border border-stone-200 bg-stone-50 px-2.5 py-1 text-[11px] font-semibold text-stone-700">{children}</span>}
function Control({label,name,options}:{label:string;name:string;options:readonly (readonly [string,string])[]}){return <label className="text-xs font-semibold text-stone-300">{label}<select aria-label={label} name={name} className="mt-2 min-h-11 w-full rounded-xl border border-white/20 bg-white/10 px-3 text-sm text-white">{options.map(([value,text])=><option className="text-stone-950" value={value} key={value}>{text}</option>)}</select></label>}
function percent(value:number){return `${(value*100).toFixed(1)}%`}
function title(value:string){return value.replaceAll("_"," ").replaceAll("-"," ").replace(/\b\w/g,letter=>letter.toUpperCase())}
function date(value:string){return new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric",timeZone:"UTC"}).format(new Date(value.length===10?`${value}T00:00:00Z`:value))}
function range(from:string,to:string){return `${date(from)} – ${date(to)}`}
function dateTime(value:string){return new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",hour:"numeric",minute:"2-digit",timeZone:"UTC"}).format(new Date(value))}
