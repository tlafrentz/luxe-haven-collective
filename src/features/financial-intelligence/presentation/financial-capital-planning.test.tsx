import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{FinancialCapitalPlanningEmpty,FinancialCapitalPlanningErrorView,FinancialCapitalPlanningSkeleton}from"./financial-capital-planning";describe("capital presentation states",()=>{it("renders no-plan state without claiming zero capital",()=>{const html=renderToStaticMarkup(<FinancialCapitalPlanningEmpty/>);expect(html).toContain("No capital plan exists");expect(html).not.toContain("$0")});it("renders accessible loading and typed errors",()=>{expect(renderToStaticMarkup(<FinancialCapitalPlanningSkeleton/>)).toContain('aria-busy="true"');expect(renderToStaticMarkup(<FinancialCapitalPlanningErrorView code="concurrency" message="Reload"/>)).toContain('role="alert"')})});
