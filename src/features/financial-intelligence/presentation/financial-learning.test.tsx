import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{FinancialLearningEmpty,FinancialLearningErrorView,FinancialLearningSkeleton}from"./financial-learning";describe("financial learning presentation states",()=>{it("renders the intentional empty state",()=>{expect(renderToStaticMarkup(<FinancialLearningEmpty/>)).toContain("No completed financial reviews are available")});it("renders accessible loading and errors",()=>{expect(renderToStaticMarkup(<FinancialLearningSkeleton/>)).toContain('aria-busy="true"');expect(renderToStaticMarkup(<FinancialLearningErrorView code="evidence" message="Incomplete"/>)).toContain('role="alert"')})});
