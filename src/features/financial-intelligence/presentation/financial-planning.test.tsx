import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{FinancialPlanningEmpty,FinancialPlanningErrorView,FinancialPlanningSkeleton}from"./financial-planning";describe("planning presentation states",()=>{it("renders intentional empty state",()=>{expect(renderToStaticMarkup(<FinancialPlanningEmpty/>)).toContain("No approved budget exists")});it("renders accessible loading and error states",()=>{expect(renderToStaticMarkup(<FinancialPlanningSkeleton/>)).toContain('aria-busy="true"');expect(renderToStaticMarkup(<FinancialPlanningErrorView code="currency" message="Mismatch"/>)).toContain('role="alert"')})});
