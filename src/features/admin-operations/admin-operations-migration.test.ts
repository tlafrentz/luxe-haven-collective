import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260802030000_admin_operations_foundation.sql","utf8");describe("admin operations persistence",()=>{it("creates separate canonical records with RLS and append-only guards",()=>{for(const table of["sync_attempts","provider_health_observations","admin_audit_events","support_tickets","support_ticket_messages","support_ticket_activity"])expect(sql).toContain(`create table public.${table}`);expect(sql.match(/enable row level security/g)?.length).toBeGreaterThanOrEqual(7);expect(sql).toContain("sync_attempts_append_only");expect(sql).toContain("admin_audit_append_only");expect(sql).toContain("support_activity_append_only");});it("backfills only canonical Hospitable runs",()=>{expect(sql).toContain("from public.integration_sync_runs r");expect(sql).toContain("hospitable-sync-run:");expect(sql).not.toContain("property-sync-error.log");});});
