import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const sql=readFileSync("supabase/migrations/20260817220000_controlled_extraction_launch.sql","utf8").toLowerCase();
describe("controlled extraction launch persistence",()=>{it("is one-shot, Admin-readable, and terminally immutable",()=>{expect(sql).toContain("unique(operation_code)");expect(sql).toContain("admins read controlled extraction launches");expect(sql).toContain("controlled_extraction_launch_closed");expect(sql).toContain("before delete")});it("contains identifiers and sanitized lifecycle state but no secrets or content",()=>{for(const field of["verification_run_id","actor_id","customer_account_id","entitlement_id","correlation_id","closed_at"])expect(sql).toContain(field);expect(sql).not.toMatch(/prompt|response_text|api_key|authorization_header|source_content/)})});
