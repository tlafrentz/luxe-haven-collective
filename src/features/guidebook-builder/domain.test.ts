import{describe,expect,it}from"vitest";import{autosaveDelay,compatibleComponents,guidebookHealth}from"./domain";import type{GuidebookDraft}from"@/features/guidebook-studio";const draft:GuidebookDraft={guidebookId:"g",workspaceId:"w",propertyId:"p",schemaVersion:"guidebook-draft.v1",revision:1,title:"Mesa",description:"",persistedAt:"",persistedBy:"a",sections:[{id:"s",name:"Welcome",visible:true,position:0,blocks:[]}]};describe("guidebook builder",()=>{it("filters compatible components before insertion",()=>{expect(compatibleComponents("Departure").some(c=>c.key==="checklist")).toBe(true);expect(compatibleComponents("Departure").some(c=>c.key==="contact")).toBe(false)});it("blocks publishing when required health checks fail",()=>{const health=guidebookHealth(draft);expect(health.publishable).toBe(false);expect(health.issues.some(i=>i.code==="missing_wifi")).toBe(true)});it("autosaves dirty changes at thirty seconds",()=>expect(autosaveDelay(true)).toBe(30_000))});
