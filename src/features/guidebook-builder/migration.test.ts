import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260806051000_lhc_gs_005a_builder_workspace.sql"),"utf8");describe("GS-005A migration",()=>{it("persists collaboration-ready sessions, validation and safe activity",()=>{expect(sql).toContain("guidebook_builder_sessions");expect(sql).toContain("guidebook_builder_validation_runs");expect(sql).toContain("guidebook_builder_activity")});it("keeps builder state workspace scoped",()=>expect(sql).toContain("workspace_id=auth.uid()"));it("supports every preview mode",()=>{for(const mode of["desktop","tablet","mobile","pdf","guest_portal"])expect(sql).toContain(`'${mode}'`)})});
