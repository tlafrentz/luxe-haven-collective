"use client";
import { useState, useTransition } from "react";
import { saveNotificationPreferencesAction, saveWorkspacePreferencesAction, type SettingsActionResult } from "@/app/actions/workspace-notifications-preferences";
import { notificationCategories, type UserWorkspacePreferences } from "../domain/notifications-preferences";
import type { EffectiveSettings } from "../application/notifications-preferences-services";

const inputClass = "ui-control mt-2 w-full px-3 text-sm";
export function NotificationPreferencesForm({ settings }: { settings: EffectiveSettings }) {
  const [value, setValue] = useState(settings.notifications), [baseline, setBaseline] = useState(settings.notifications);
  const [result, setResult] = useState<SettingsActionResult | null>(null), [pending, start] = useTransition();
  const dirty = JSON.stringify(value) !== JSON.stringify(baseline);
  const subscription = (category: string) => value.subscriptions.find((item) => item.category === category);
  const setFrequency = (category: typeof notificationCategories[number], frequency: string) => setValue((current) => ({ ...current, subscriptions: current.subscriptions.map((item) => item.category === category ? { ...item, frequency: frequency as typeof item.frequency } : item) }));
  return <form onSubmit={(event) => { event.preventDefault(); start(async () => { const response = await saveNotificationPreferencesAction({ value, expectedRevision: value.revision, commandId: crypto.randomUUID() }); setResult(response); if (response.ok) { const next = { ...value, revision: response.revision ?? value.revision }; setValue(next); setBaseline(next); } }); }} className="space-y-8">
    <section><h2 className="text-xl font-semibold">Delivery channels</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">
      <label className="rounded-xl border p-4"><input type="checkbox" checked={value.channels.inApp} onChange={(e) => setValue({ ...value, channels: { ...value.channels, inApp: e.target.checked } })} /> <strong>In app</strong><span className="block text-sm text-stone-600">Canonical notification record and action links.</span></label>
      <label className="rounded-xl border p-4"><input type="checkbox" checked={value.channels.email} onChange={(e) => setValue({ ...value, channels: { ...value.channels, email: e.target.checked } })} /> <strong>Email</strong><span className="block text-sm text-stone-600">Urgent alerts, access changes, and selected digests.</span></label>
    </div></section>
    <section><h2 className="text-xl font-semibold">Required notifications</h2><p className="mt-2 text-sm text-stone-600">Access changes, suspension, provider authorization expiry, disconnection, and critical security events remain enabled.</p><div className="mt-3 rounded-xl bg-amber-50 p-4 text-sm font-semibold text-amber-950">Required · cannot be turned off</div></section>
    <section><h2 className="text-xl font-semibold">Subscriptions</h2><div className="mt-4 divide-y rounded-xl border">{notificationCategories.map((category) => <label key={category} className="grid gap-2 p-4 sm:grid-cols-[1fr_13rem] sm:items-center"><span className="font-medium capitalize">{category.replaceAll("-", " & ")}</span><select aria-label={`${category} frequency`} className={inputClass} value={subscription(category)?.frequency ?? "off"} onChange={(e) => setFrequency(category, e.target.value)}><option value="immediate">Immediate</option><option value="daily-digest">Daily digest</option><option value="weekly-digest">Weekly digest</option><option value="off">Off</option></select></label>)}</div></section>
    <section className="grid gap-5 sm:grid-cols-2"><label>Notification timezone<select className={inputClass} value={value.timezone} onChange={(e) => setValue({ ...value, timezone: e.target.value })}>{["America/Chicago","America/Phoenix","America/New_York","America/Denver","America/Los_Angeles"].map((zone) => <option key={zone}>{zone}</option>)}</select></label>
      <label>Digest schedule<select className={inputClass} value={value.digest.frequency} onChange={(e) => setValue({ ...value, digest: { ...value.digest, frequency: e.target.value as "daily"|"weekly"|"off" } })}><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="off">Off</option></select></label>
      <label className="rounded-xl border p-4 sm:col-span-2"><input type="checkbox" checked={value.quietHours.enabled} onChange={(e) => setValue({ ...value, quietHours: { ...value.quietHours, enabled: e.target.checked } })} /> <strong>Quiet hours</strong><span className="block text-sm text-stone-600">{value.quietHours.start}–{value.quietHours.end} · critical alerts may still be delivered.</span></label>
    </section>
    <Save dirty={dirty} pending={pending} result={result} label="Save Notification Settings" />
  </form>;
}
export function WorkspacePreferencesForm({ settings }: { settings: EffectiveSettings }) {
  const [value, setValue] = useState(settings.preferences), [baseline, setBaseline] = useState(settings.preferences);
  const [result, setResult] = useState<SettingsActionResult | null>(null), [pending, start] = useTransition();
  const dirty = JSON.stringify(value) !== JSON.stringify(baseline);
  const update = <K extends keyof UserWorkspacePreferences>(key: K, next: UserWorkspacePreferences[K]) => setValue({ ...value, [key]: next });
  return <form onSubmit={(event) => { event.preventDefault(); start(async () => { const response = await saveWorkspacePreferencesAction({ value, expectedRevision: value.revision, commandId: crypto.randomUUID() }); setResult(response); if (response.ok) { const next = { ...value, revision: response.revision ?? value.revision }; setValue(next); setBaseline(next); } }); }} className="space-y-8">
    <Section title="Regional"><label>Personal timezone<select className={inputClass} value={value.timezone ?? ""} onChange={(e) => update("timezone", e.target.value || undefined)}><option value="">Inherited from Organization</option>{["America/Chicago","America/Phoenix","America/New_York","America/Denver","America/Los_Angeles"].map((zone) => <option key={zone}>{zone}</option>)}</select></label><label>Locale<select className={inputClass} value={value.locale} onChange={(e) => update("locale", e.target.value)}><option value="en-US">English (United States)</option></select></label></Section>
    <Section title="Workspace Context"><label>Default landing page<select className={inputClass} value={value.defaultLandingPage} onChange={(e) => update("defaultLandingPage", e.target.value as UserWorkspacePreferences["defaultLandingPage"])}>{([["home","Home"],["workspace","Workspace"],["bookings","Bookings"],["actions","Action Center"],["intelligence","Intelligence"]] as const).map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Default property<select className={inputClass} value={value.defaultPropertyId ?? ""} onChange={(e) => setValue({ ...value, defaultPropertyMode: e.target.value ? "specific" : "all-accessible", defaultPropertyId: e.target.value || undefined })}><option value="">All accessible properties</option>{settings.properties.map((property) => <option key={property.id} value={property.id}>{property.name}</option>)}</select></label></Section>
    <Section title="Display"><label>Density<select className={inputClass} value={value.density} onChange={(e) => update("density", e.target.value as "comfortable"|"compact")}><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></label><label>Motion<select className={inputClass} value={value.motion} onChange={(e) => update("motion", e.target.value as "system"|"reduced")}><option value="system">System preference</option><option value="reduced">Reduced motion</option></select></label></Section>
    <Section title="Workflow"><label>Intelligence detail<select className={inputClass} value={value.intelligenceDetail} onChange={(e) => update("intelligenceDetail", e.target.value as UserWorkspacePreferences["intelligenceDetail"])}><option value="summary">Summary</option><option value="standard">Standard</option><option value="detailed">Detailed</option></select></label></Section>
    <Save dirty={dirty} pending={pending} result={result} label="Save Preferences" />
  </form>;
}
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section><h2 className="text-xl font-semibold">{title}</h2><div className="mt-4 grid gap-5 sm:grid-cols-2">{children}</div></section>; }
function Save({ dirty, pending, result, label }: { dirty: boolean; pending: boolean; result: SettingsActionResult|null; label: string }) { return <div className="sticky bottom-4 rounded-2xl border bg-white/95 p-4 shadow-lg backdrop-blur"><div aria-live="polite" className="mb-2 text-sm">{result?.message ?? (dirty ? "You have unsaved changes." : "All changes saved.")}</div><button disabled={!dirty || pending} className="rounded-full bg-stone-950 px-5 py-2.5 font-semibold text-white disabled:opacity-40">{pending ? "Saving…" : label}</button></div>; }
