import type { ActionCenterSummary } from "../domain";
const items = [{ key: "ready", label: "Ready" }, { key: "inProgress", label: "In progress" }, { key: "blocked", label: "Blocked" }, { key: "completed", label: "Completed" }] as const;
export function ExecutionSummary({ summary }: { summary: ActionCenterSummary }) { return <section aria-label="Execution overview" className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map((item) => <div key={item.key} className="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-stone-500">{item.label}</p><p className="mt-5 text-3xl font-semibold text-stone-950">{summary[item.key]}</p></div>)}</section>; }
