import type { ActionCenterPriorityView } from "../domain";
const classes: Record<ActionCenterPriorityView, string> = { critical: "border-red-500 bg-red-50 text-red-800", high: "border-amber-300 bg-amber-50 text-amber-800", normal: "border-stone-300 bg-white text-stone-600", low: "border-stone-200 bg-stone-50 text-stone-500", deferred: "border-slate-200 bg-slate-50 text-slate-500" };
export function ActionPriorityBadge({ priority }: { priority: ActionCenterPriorityView }) { return <span className={`inline-flex rounded-full border px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.12em] ${classes[priority]}`}>{priority}</span>; }
