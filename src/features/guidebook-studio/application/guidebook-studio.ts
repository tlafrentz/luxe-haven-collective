import type { Guidebook, GuidebookVersion, PropertyGuidebookSnapshot, PublishedGuidebook } from "../domain";
export interface GuidebookRepository{list(workspaceId:string):Promise<readonly Guidebook[]>;get(workspaceId:string,id:string):Promise<Guidebook|null>;getByPublicSlug(slug:string):Promise<Readonly<{guidebook:Guidebook;version:GuidebookVersion}>|null>;saveDraft(guidebook:Guidebook,expectedRevision:number):Promise<Guidebook>;publish(input:{guidebook:Guidebook;version:GuidebookVersion;expectedRevision:number}):Promise<void>;}
export interface GuidebookPropertyPort{get(workspaceId:string,propertyId:string):Promise<PropertyGuidebookSnapshot|null>}
export type GuidebookStudio=Readonly<{guidebooks:readonly Guidebook[];properties:readonly Readonly<{id:string;name:string;hasGuidebook:boolean}>[];entitlements:Readonly<{create:boolean;publish:boolean;host:boolean}>;evaluatedAt:string}>;
export async function getGuidebookStudio(deps:{repository:GuidebookRepository;properties:readonly Readonly<{id:string;name:string}>[];entitlements:GuidebookStudio["entitlements"]},workspaceId:string):Promise<GuidebookStudio>{const guidebooks=await deps.repository.list(workspaceId);return Object.freeze({guidebooks,properties:Object.freeze(deps.properties.map(property=>({...property,hasGuidebook:guidebooks.some(g=>g.propertyId===property.id)}))),entitlements:Object.freeze(deps.entitlements),evaluatedAt:new Date().toISOString()})}
export async function getPublishedGuidebook(repository:GuidebookRepository,slug:string):Promise<PublishedGuidebook|null>{const found=await repository.getByPublicSlug(slug);if(!found)return null;return Object.freeze({...found.version.snapshot,publicSlug:found.guidebook.publicSlug,version:found.version.version,publishedAt:found.version.publishedAt})}
