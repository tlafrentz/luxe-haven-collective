import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260806050000_lhc_gs_005_template_library.sql"),"utf8");
describe("GS-005 migration",()=>{it("persists versions, component styling, channels, branding, usage, and audit",()=>{for(const table of ["guidebook_template_versions","guidebook_template_component_styles","guidebook_template_channels","guidebook_template_branding_overrides","guidebook_template_usage","guidebook_template_audit_events"])expect(sql).toContain(table)});it("protects published versions and scopes workspace data",()=>{expect(sql).toContain("published_template_version_immutable");expect(sql).toContain("workspace_id=auth.uid()")});it("seeds only Mesa Modern",()=>{expect(sql).toContain("'mesa-modern','Mesa Modern'");expect(sql).not.toContain("'luxury','Luxury'")})});
