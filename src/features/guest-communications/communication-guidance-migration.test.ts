import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const migration=readFileSync("supabase/migrations/20260726200000_communication_guidance_templates.sql","utf8");
describe("COM-005A persistence",()=>{it("versions and protects published templates",()=>{expect(migration).toContain("guest_communication_template_series_version_key");expect(migration).toContain("published communication template versions are immutable");expect(migration).toContain("delivery_mode in('immediate','scheduled','manual-hold')");});it("retains recommendation dispositions and append-only guidance history",()=>{expect(migration).toContain("guest_communication_recommendations");expect(migration).toContain("dismissal_reason");expect(migration).toContain("guest_guidance_activity_append_only");});});
