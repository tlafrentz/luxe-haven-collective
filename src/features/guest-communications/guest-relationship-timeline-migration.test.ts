import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const migration=readFileSync("supabase/migrations/20260726210000_guest_relationship_timeline.sql","utf8");
describe("COM-006A persistence",()=>{it("uses an immutable, guest-owned, deterministically ordered stream",()=>{expect(migration).toContain("guest_relationship_events");expect(migration).toContain("sequence bigint generated always as identity");expect(migration).toContain("guest_relationship_events_immutable");expect(migration).not.toContain("event_type text not null check");});it("captures communication, reservation, maintenance, and guidebook sources",()=>{for(const trigger of["guest_message_relationship_event","guest_delivery_relationship_event","guest_booking_relationship_event","guest_maintenance_relationship_event","guest_guidebook_relationship_event"])expect(migration).toContain(trigger);});it("supports workspace-isolated indexed queries and idempotent source capture",()=>{expect(migration).toContain("guest_relationship_timeline_idx");expect(migration).toContain("can_access_workspace_property");expect(migration).toContain("unique(workspace_id,source_type,source_id,source_version,event_type)");});});
