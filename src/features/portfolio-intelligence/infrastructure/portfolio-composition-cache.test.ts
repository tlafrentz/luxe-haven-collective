import{describe,expect,it}from"vitest";import{overviewProjectionFixture,overviewPropertyFixture}from"../application/overview/overview.test";import{buildPortfolioComposition}from"../application/composition";import{compositionInputFixture}from"../application/composition/composition.test";import{AuthorizationAwarePortfolioCompositionCache,portfolioCompositionCacheKey}from"./portfolio-composition-cache";
const context={workspaceId:"w",membershipId:"m",role:"owner",propertyIds:["a"],from:"2026-07-01",to:"2026-07-31",comparisonType:"previous-period",projectionVersion:"v1"};const model=buildPortfolioComposition({projection:overviewProjectionFixture([overviewPropertyFixture("a",100)]),input:compositionInputFixture});
describe("Composition cache",()=>{it("isolates authorization, scope, periods, and projection versions",()=>{for(const variant of[{membershipId:"x"},{propertyIds:["b"]},{to:"2026-08-31"},{projectionVersion:"v2"}])expect(portfolioCompositionCacheKey(context)).not.toBe(portfolioCompositionCacheKey({...context,...variant}));});it("invalidates all workspace variants",()=>{const cache=new AuthorizationAwarePortfolioCompositionCache();cache.set(context,model);expect(cache.get({...context,role:"viewer"})).toBeNull();cache.invalidateWorkspace("w");expect(cache.get(context)).toBeNull();});});
