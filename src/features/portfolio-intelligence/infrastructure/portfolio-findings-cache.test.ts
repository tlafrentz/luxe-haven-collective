import{describe,expect,it}from"vitest";import{buildPortfolioFindings}from"../application/findings";import{findingsQueryFixture}from"../application/findings/findings.test";import{AuthorizationAwarePortfolioFindingsCache,portfolioFindingsCacheKey}from"./portfolio-findings-cache";const context={workspaceId:"w",membershipId:"m",role:"owner",propertyIds:["a"],from:"2026-07-01",to:"2026-07-31",comparisonType:"previous-period",capabilities:["performance","financial"],projectionVersion:"v1",comparisonPolicyVersion:"c1",compositionPolicyVersion:"d1",findingsPolicyVersion:"e1"};const value=buildPortfolioFindings(findingsQueryFixture());describe("Findings cache",()=>{it("isolates authorization, capabilities, scope, periods, and upstream policies",()=>{for(const variant of[{membershipId:"x"},{propertyIds:["b"]},{capabilities:["performance"]},{to:"2026-08-31"},{compositionPolicyVersion:"d2"},{findingsPolicyVersion:"e2"}])expect(portfolioFindingsCacheKey(context)).not.toBe(portfolioFindingsCacheKey({...context,...variant}));});it("does not serve full financial findings to a restricted membership",()=>{const cache=new AuthorizationAwarePortfolioFindingsCache();cache.set(context,value);expect(cache.get({...context,role:"viewer",capabilities:["performance"]})).toBeNull();cache.invalidateWorkspace("w");expect(cache.get(context)).toBeNull();});});
