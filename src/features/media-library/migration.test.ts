import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20260806040000_lhc_gs_004_media_library.sql","utf8");describe("GS-004 persistence",()=>{it("stores derivatives, usage, collections, jobs, and archive lineage",()=>{for(const value of["guidebook_media_derivatives","guidebook_media_usage","guidebook_media_collection_items","guidebook_media_processing_jobs","guidebook_media_archive_events","replacement_media_id"])expect(sql).toContain(value)});it("enforces immutable published versions and idempotent processing",()=>{expect(sql).toContain("media_version_immutable");expect(sql).toContain("idempotency_key")});it("enforces RLS and blocks anonymous records",()=>{expect(sql).toContain("enable row level security");expect(sql).toContain("from anon")})});
