import{describe,expect,it}from"vitest";import{authorizeMedia,validateCollection,validateReplacement,type MediaContext}from"./application";import type{MediaAsset,MediaVersion}from"./domain";const context:MediaContext={workspaceId:"w",actorId:"u",permissions:new Set(["media.edit"]),propertyIds:new Set(["p"]),isPlatformAdmin:false},asset={id:"a",workspaceId:"w",propertyId:"p",scope:"property"}as MediaAsset,version={id:"v1",assetId:"a",versionNumber:1}as MediaVersion;describe("media application",()=>{it("enforces permission and scope",()=>{expect(()=>authorizeMedia(context,"media.edit",asset)).not.toThrow();expect(()=>authorizeMedia(context,"media.approve",asset)).toThrow("MEDIA_PERMISSION_DENIED");expect(()=>authorizeMedia(context,"media.edit",{...asset,propertyId:"other"})).toThrow("MEDIA_PROPERTY_SCOPE_DENIED")});it("validates collection ownership",()=>{expect(()=>validateCollection({id:"c",workspaceId:null,propertyId:null,name:"x",slug:"x",description:"",scope:"workspace",createdBy:"u",createdAt:"now"})).toThrow("MEDIA_COLLECTION_WORKSPACE_REQUIRED")});it("replaces with a new version and preserves publications",()=>{expect(validateReplacement(asset,version,{...version,id:"v2",versionNumber:2},[])).toMatchObject({historicalPublicationsPinned:true,newDraftVersion:2})})});
