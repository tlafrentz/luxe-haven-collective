# QA Review — portal — 2026-08-03

## Scope
- Routes reviewed: 0 of 5 (`node .claude/skills/qa-review/scripts/discover-routes.mjs --area=portal`) — all require a signed-in session
- Routes skipped: 5
- Commit reviewed: fd002c0c

## Summary
No content findings — this run had no credentials to sign in with, so only the auth boundary itself was verified (see below). Re-run with real portal-user credentials to actually review these screens.

## What was verified
The auth boundary was checked directly rather than visiting all 5 routes individually, since they're gated by the same middleware and would all produce the same result: `/bookings` was navigated to while unauthenticated and correctly redirected to `/login?next=%2Fbookings` (verified via `window.location.href`), preserving the original destination for post-login redirect. This confirms `middleware.ts` + `isProtectedRoute()` (`src/lib/auth/roles.ts:10`) are working correctly for this area.

## Routes skipped
All 5 portal routes require a `guest`/`owner`/`cleaner`-role signed-in session (`auth-required`), and no test credentials are available:
- `/bookings`
- `/guidebooks`
- `/messages`
- `/properties`
- `/reports`

To review this area, supply real portal-user credentials and re-run this skill scoped to `--area=portal`.
