# QA Review — hpm — 2026-08-03

## Scope
- Routes reviewed: 1 of 1 (`node .claude/skills/qa-review/scripts/discover-routes.mjs --area=hpm`)
- Routes skipped: 0
- Commit reviewed: fd002c0c

## Summary
Critical: 0 · High: 1 · Medium: 0 · Low: 0

## Findings

### `/hpm`
**[High] [Functionality]** Footer "Privacy Policy" and "Terms of Service" links both 404
- What: The footer links to `/privacy` and `/terms`. Neither route exists — navigating to either returns Next.js's default 404 page ("This page could not be found").
- Evidence: `src/features/marketing/hpm/components/hpm-footer.tsx:137` (`href="/privacy"`) and `:144` (`href="/terms"`); confirmed live, both URLs 404. This is specific to the `/hpm` page's own footer component — the main site footer (used on `/`, `/about`, etc.) doesn't link to either page, so this isn't a site-wide broken link, just this one page's footer.
- Why it matters: broken legal-page links are a trust/compliance issue, not just a dead link — a visitor (especially a prospective "founding partner" being asked to commit to an early-access program on this exact page) clicking through to check the privacy policy or terms hits a 404 instead.
- Suggested fix: either build `/privacy` and `/terms` pages, or point these links at wherever the real policy currently lives (if it exists elsewhere) until dedicated pages exist.

## What worked well
- Unlike most other marketing pages, `/hpm` has its own correct, non-duplicated page title (`"Hospitality Performance Management | Luxe Haven Collective"`) — it doesn't hit the title-duplication bug documented in the marketing report, because its title string doesn't include the manual `" | Luxe Haven Collective"` suffix that causes that bug elsewhere.
- Long, content-heavy page (founder letter, FAQ accordion, multiple sections) rendered cleanly with no console errors and no client-side network calls.
- In-page anchor navigation (`#why-hpm`, `#platform`, `#vision`, `#partners`, `#about`, `#faq`) and all `/contact?service=founding-partner` CTAs point to real, existing destinations.

## Routes skipped
- None.
