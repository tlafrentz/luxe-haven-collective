# Workspace Onboarding

The founding-owner path derives required and recommended tasks from real configuration: identity, organization, property, provider, first sync, owner membership, notifications, and final review. Required system conditions cannot be manually checked off. Invited members see only personal or authorized tasks.

Completion history is preserved. A material regression reopens onboarding as `Needs Attention` without replaying the original welcome flow.
