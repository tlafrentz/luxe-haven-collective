# Report template policy

Activated templates are immutable. Typography, page configuration, section order, branding, and required sections are versioned. A structural or visual change creates a new version.

Templates may hide approved optional sections but cannot remove required sections, calculate values, loosen redaction, or change source conclusions. Historical reports continue using their recorded template version.
