# Stripe Local Development

Use Stripe test mode or a Stripe Sandbox with a restricted key. Configure the Investment Analysis test Price ID, start the app, authenticate, and initiate checkout from its product page.

Validate one-time and subscription modes with mocked or test Price records. The return page must say processing—not paid. Cancellation must preserve a non-paid pending order. Do not create entitlements in this milestone.
