# Internal Transfer Policy

A transfer is eliminated from consolidated economic cash flow only when an inflow and outflow share a transfer reference, amount, currency, and distinct included accounts. Both sides are retained for authorized account-level inspection.

Unmatched transfer candidates remain visible as unknown activity and reduce transfer coverage and confidence. Matching or unmatching invalidates affected cached statements. Transfers across excluded scope boundaries are not silently treated as consolidated internal transfers.
