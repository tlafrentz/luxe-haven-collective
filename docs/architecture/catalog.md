# Catalog Projection

`getCommerceCatalog` returns active public products, active prices, and active offers. `getCommerceAdministration` returns full catalog history. `getCommerceProduct` resolves product identity and versioned prices.

Presentation formats these projections and performs no eligibility, pricing, entitlement, fulfillment, or order calculations.
