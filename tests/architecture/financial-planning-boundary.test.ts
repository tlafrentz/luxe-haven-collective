import{describe,expect,it}from"vitest";import fs from"node:fs";import path from"node:path";describe("planning presentation boundary",()=>{it("consumes GetFinancialPlanning projection without persistence or calculations",()=>{const source=fs.readFileSync(path.join(process.cwd(),"src/features/financial-intelligence/presentation/financial-planning.tsx"),"utf8");expect(source).not.toMatch(/\b(SELECT\s+\*|FROM\s+budgets|repository|buildVariance|buildFinancialPlanning)\b/i);expect(source).toContain("FinancialPlanning")})});
