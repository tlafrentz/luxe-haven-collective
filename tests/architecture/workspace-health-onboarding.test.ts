import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
const sql=readFileSync("supabase/migrations/20260725170000_workspace_health_onboarding.sql","utf8");
describe("Sprint 4F validation architecture",()=>{it("persists history without duplicating capability truth",()=>{expect(sql).toContain("workspace_onboarding");expect(sql).toContain("workspace_health_issues");expect(sql).not.toContain("workspace_health_snapshots");});it("checks grants, RLS, and core integrity",()=>{expect(sql).toContain("enable row level security");expect(sql).toContain("grant select");expect(sql).toContain("Owner without valid profile");expect(sql).toContain("Orphan active property");});it("protects issue semantics",()=>{expect(sql).toContain("This issue cannot be dismissed");expect(sql).toContain("Active workspace membership is required");});});
