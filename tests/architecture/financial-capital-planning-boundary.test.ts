import{describe,expect,it}from"vitest";import fs from"node:fs";import path from"node:path";describe("capital presentation boundary",()=>{it("consumes the bounded read model without financial, conflict, or allocation calculations",()=>{const source=fs.readFileSync(path.join(process.cwd(),"src/features/financial-intelligence/presentation/financial-capital-planning.tsx"),"utf8");expect(source).not.toMatch(/\b(SELECT\s+\*|FROM\s+capital|repository|calculateCapitalPosition|detectCapitalConflicts|evaluateCapitalCandidates)\b/i);expect(source).toContain("FinancialCapitalPlanning")})});
