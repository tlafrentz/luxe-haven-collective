import{describe,expect,it}from"vitest";import fs from"node:fs";import path from"node:path";describe("financial learning presentation boundary",()=>{it("consumes GetFinancialLearning without outcome calculations or repositories",()=>{const source=fs.readFileSync(path.join(process.cwd(),"src/features/financial-intelligence/presentation/financial-learning.tsx"),"utf8");expect(source).not.toMatch(/\b(SELECT\s+\*|FROM\s+outcomes|repository|evaluateFinancialOutcome|evaluateForecastAccuracy|buildFinancialLearning)\b/i);expect(source).toContain("FinancialLearning")})});
